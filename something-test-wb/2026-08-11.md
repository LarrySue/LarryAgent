# 2026-08-11

## 项目全景 review（只读，未改动代码）

通读了 README / TODO / backend 全部核心代码，形成判断：

- 状态：P0-P2 全闭环 + P3.1 流式已通，P3.2+ 待做，P4/P5 未开始（Tauri 仅骨架）。
- 架构：FastAPI + SQLite(aiosqlite) + ChromaDB(内嵌) + bge-small-zh 本地 embedding；分层清晰（api/services/db/memory/rag/tools/models）。
- 亮点：测试 7 文件约 1900 行；降级保护（ChromaDB 挂了 SQLite 记录保留）；工具安全（路径沙箱/IP 白名单/黑名单/超时杀进程树）。

## 发现的关键问题（待用户处理）

1. **回归 bug（确定）**：P2.3 将 `chat_completion` 返回从 `str` 改为 `LLMResponse`（含 usage/tool_calls），但 `memory/archiver.py::generate_summary` 未同步——`len(summary)` 会抛 `TypeError`，且 `api/memory.py::trigger_archive` 的 `ArchiveResponse(summary=str)` 收 dataclass 会 ValidationError。`/api/memory/archive` 现在应会 500。修复需改 `summary.content`。
2. ~~API Key 泄漏~~ **判断有误，已澄清（2026-08-12）**：`backend/config.yaml` 确含真实 DeepSeek key，但该文件在 `.gitignore`（第 14 行）中，`git ls-files` 与 `git log --all` 均确认其从未入库；仓库只追踪 `config.example.yaml`（api_key 为空模板）。教训：review 时读到敏感内容后应先 `git ls-files`/`git log` 验证是否入库，再下"泄漏"结论。该条撤销。
3. **LLM 双倍调用**：`chat_service._chat_flow` 最终回复路径先非流式探测 finish_reason 再流式重生成文本，token 成本翻倍。可让流式路径直接复用非流式内容或改造。
4. 安全边界：shell 黑名单可绕过（TODO 已自知），当前靠 IP 白名单兜底；P5 放开局域网前必须加 API Key 校验（P3.4）。
