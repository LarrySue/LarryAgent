# 2026-09-09 工作日志

## 老大再确认的运行环境前提（含 WB 核实出的含糊点 · 待拍板）

- **老大前提（原话意）**：**Windows 仅作开发 + 测试**，DSH 实际使用运行在**服务器 Linux** 上 → Win 下存在问题可接受、可以简陋。"反复确认过"。
- **WB 核实结论：该前提本身成立，但文档中①未作为显式口径写下；②存在一处与之潜在冲突的含糊点，须老大拍板。**
  - **已按该前提走的部分（无冲突）**：
    * §3.3 `sandbox/` → 2.7.1 升 ✅ 明确标注"**仅 Linux/macOS 侧成立**"
    * §3.4 Windows 官方 CLI segfault → 只影响 B1 挂载（开发工具面），按前提可容忍（改手工放置插件）
    * 云端部署/租户隔离本就定为自做
  - **⚠️ 含糊点：2.10.2 端侧执行器到底用不用 DSH 的 Windows 沙箱？**
    * 承接总表 2.10.2 行：「**自做下沉**」但同格又写「Windows 沙箱后端已确认存在；待 DSH-2 实测」——**两句口径不一致**
    * DSH-2 退出条件③：Windows 端 `ctx.sandbox` provider 可用性，标注「**2.10.2 端侧执行器前提**」→ 预设了要用
    * DSH-6 验收五层⑤：「Windows 端侧执行器验收（**若走** 2.10.2，与 DSH-2 实测项③同源）」→ 又退回未定
    * §3.6 前端路线：「Tauri 壳是 2.10.2 端侧执行器的宿主」
  - **为何关键**：端侧执行器跑在**老大自己的 Windows 机器**上（file_ops/shell 操作本机文件，PC 版 C/S 选型的根本理由）。
    * 若**用** DSH Windows sandbox → Windows 是**生产环境**，segfault / fail-closed / 提权流程均不可"简陋"
    * 若**不用**（Tauri 原生自做）→ 老大前提完全成立，Windows 退回纯开发测试
- **建议（待老大拍板后执行）**：把"Windows 仅开发/测试、其缺陷不按生产标准处理"作为**显式口径**写进决策稿 §3.4 或 §3.6，并同步澄清 2.10.2 归属——**现在没写，未来 AI 可能按相反理解执行**。

## 老大定调：S 侧是根基，C 侧是薄层（架构权重分配）

- **老大原话意**：「S 侧只要不出问题，C 侧怎么都好做——无非几个界面几个简单功能模块，哪天想推翻重做都没问题。**S 侧是根基、重中之重：数据、模型、记忆、知识、配置、归档、会话都在那边**」。
- **WB 认同，且该判断有据可依**：C 侧重做成本 ≈ 界面 + 少量模块；S 侧出问题 = 数据/记忆/会话/归档全盘受损，两者不可比。**与 A-framework 主战场一致**（贴近的是 DSH 核心层，核心层在 S 侧）。
- **⚠️ 该定调成立的前提（WB 补，须守住）**：**C 侧不得承载 DSH 运行时**。若端侧执行器走 DSH 的 Windows sandbox（+ answerer 回调 + 事件流），C 侧就从薄层变成"嵌了框架运行时的宿主"，**推翻重做成本与 S 侧绑定**，老大的定调随之失效。
- **由此推出的建议（待老大定）**：**2.10.2 端侧执行器走 Tauri 原生自做，不引 DSH Windows sandbox**。
  * 收益：C 侧保持薄层（符合定调）；绕开 Windows segfault / fail-closed / 提权流程一整坨；DSH 部署面收敛为纯 Linux server
  * 代价：三档沙箱策略（read-only / workspace-write / danger-full-access）+ 一次性提权审批须自做（工作量一次性、可控）
  * 连带解决：DSH-2 退出条件③ 的权重倒置（③ 可降级为局部选型项，不再触发全局重估）、承接总表 2.10.2 格内自相矛盾
- **性质标注**：以上建议是**从老大的权重定调推得**，非"技术上必须"——走 DSH 沙箱技术上也可行，只是与定调冲突。
- **老大拍板（终结端侧讨论）**：「DSH Windows 沙箱先用着，不影响 S 端决策，不要再纠结」。→ **端侧议题就此关闭**，WB 不再主动提。

## WB 复核：S 侧决策是否存在定位偏差（老大点名要的答案）

- **结论：S 侧（数据/模型/记忆/知识/配置/归档/会话）的**定位本身无偏差**，但**验收方法有偏差**。
  - **无偏差的部分**：S 侧大头的能力判定全部是**源码级 + 跨平台**，未被 Windows 污染——2.9.2 compaction 承接、2.8.2 trajectory/session-query SQLite FTS、2.5.1 llm/ 多模型、记忆自做挂事件流，依据均为锁定版源码与第 0 项 SDK 实测（跨平台）；§3.3 亦明确 `sandbox/`→2.7.1「升 ✅ **仅 Linux/macOS 侧成立**」。云端部署/租户隔离本就定为自做。
  - **⚠️ 真偏差：把「Windows 本机实测」当成 S 侧能力的验收依据**（S 侧生产在 Linux）。两处具体：
    * **承接总表 2.7.1**（424 行）：已写明「仅 Linux/macOS 成立」，却要求「待 **DSH-2 本机实测**后升 ✅」——本机是 Windows，**测不出 Linux 才成立的能力，或得出错误结论**
    * **DSH-2 退出条件① `storage/` 外接 SQLite**：SQLite 的文件锁 / WAL / 并发行为**在 Windows 与 Linux 有差异**，Windows 结论不能直接外推到 Linux 生产
    * （退出条件② acp 契约、⑤ embedding 向量漂移均为跨平台纯计算，**不受影响**）
  - **建议口径（待老大定是否落文档）**：DSH-2 实测一律标注「**基于 Windows 开发机**」；凡涉 OS 相关行为者标「**待 Linux 生产环境复验**」，不作为 S 侧最终判定。2.7.1 的「本机实测后升 ✅」应改为「Linux 环境实测后升 ✅」或并入 DSH-5 上云验收。
  - **附带**：文档缺「开发在 Windows / 生产在 Linux」这一环境口径（grep 无命中），建议补一句，否则后续 AI 默认"本机实测 = 生产结论"。

## 老大调整（提交 `2d90469`）

- **Trae 报告改名**：`docs/dsh/dsh-pysdk-probe.md` → `dsh-pysdk-probe-trae.md`（与 `-claude` / `-qoder` 命名对称）。git 识别为 rename，历史保留。**唯一断链**在 `dsh-migration.md:248`，已修。历史日志 `2026-09-08.md` 内旧名为陈述，按 append-only 未改。
- **老大在精简约束**（延续 2026-09-08「约束不是越多越好、不随意加码」）：
  * MEMORY.md：**删掉了「派发稿五要素」「多方并行验证三规则」**（这两条是 WB 加的操作性经验）；角色/节奏/待办分离条目去掉日期与元注释
  * docs/dsh/README.md：删规则 4「AI 报告行号不可全信」（该证据在 `dsh-form-probe-claude.md` 文件头的 WB 复核注记里仍保留，未删）
  * 三份报告头部：去「2026-09-08」日期，实质保留（永久保留 / 一等已被 §3.5 覆盖）
  * **WB 不提异议**——与老大既有裁决一致（约束已够用，不再加码；风格与操作性经验不进约束文件）
- **仍未落的两处**（老大未拍板，WB 不擅动；派 DSH-2 前建议一并处理）：
  1. 承接总表 2.10.2 格内「自做下沉」与「待 DSH-2 实测 Windows 沙箱」自相矛盾
  2. 退出条件③（Windows `ctx.sandbox`）与①②④⑤ 同处全局 go/no-go 闸门 → 端侧局部风险被抬成路线级重估触发条件（与 2.7.1「仅 Linux/macOS 成立」的结论打架）

## 探讨：DSH 纳入后的「开发时」项目结构（老大提问，边聊边定）

- **git 范围 = 只包含我们的部分**：进 git 的是 `harness/`（TS 插件源码）+ `client/` + `mobile/` + 文档区；**不进**的是 DSH 本体（npm 依赖）、`DSH_HOME`（装配点）、`node_modules`、编译产物。与现在（Python 依赖不入库）无性质区别。
  * **真正变了的点**：现在 clone 下来 `pip install` 就能跑；将来 clone 下来**跑不起来**——DSH 是**宿主**不是依赖（numpy 不装顶少少个函数，DSH 不装是整个东西不存在，无入口）。
- **工程数量没变多，但关系变了**：现在 backend + client = 2 个独立进程、HTTP 互通、各自可独立启动调试；将来 harness 不是进程，是**注入 DSH 进程的插件**，不能独立启动。
  * **调试多一层**（宿主层，我们看不见内部）：client devtools → 通信层 → **DSH 宿主进程（新增）** → cordis 加载 → 我们的插件（Node inspector attach）
  * **压缩办法（设计原则）**：**纯逻辑 + 薄适配层**——纯逻辑（记忆提取/角色组装/参数校验/检索排序）走 Vitest 单测**不起 DSH**；只有适配层（`ctx.tools` 注册 / 事件订阅 / answerer）才需要 DSH 环境。这也是 DSH-2.4 测试基建单列的理由。
- **🟢 新发现（查锁定版源码）：DSH 自带完整 C/S 应用骨架**——`host/`（HTTP server + SPA dist server）／`api/`（Client→Host typed remote calls + forwarded events）／`client/`（browser transport）／`apps/cli` 启动 `dsh-base` bundle serve `apps/web/`。即"后端+前端+远程 API 契约"它全都有，与我们的结构同构。
  * **但这不改变既有判定**：§3.6 前端路线已定死「**保留 Vue/Tauri，走 sdk / acp profile 对接，不采用 DSH Web-GUI**」→ 开发态与 PC 单机走 **stdio**（Tauri 起 DSH 子进程），**不过 HTTP**；只有上云才需 HTTP/WS 网关，而 §3.3 已定云端自做。**WB 一度误以为"S 侧 HTTP 层用谁的"是个待定分叉，实为查漏——已向老大更正。**
- **待定：开发时 DSH 本体从哪来**（决定能否调试进内部）：① npm 包（轻，但调试进去是编译产物，sourcemap 待验）② 源码构建 checkout（261 包 monorepo，重，但能进内部任意一行）。**A 案价值就在默认不需要 ②**。倾向默认 npm、需要时再开源码副本。
- **待验：模块级 HMR 可能可开**——`packages/bundle/base/cordis.patch.yml` 有 `hmr` 行（`@deepseek-ai/cordis-plugin-hmr`，默认 `disabled: true`，注释 "Module reload is opt-in per profile"）。第 0 项 Claude 判的是 **patch 配置**不可热重载（`patchReload: startup`），**模块级是另一回事**。若可开，开发体验完全不同。已作为可选项写进 DSH-2.2 派发稿（不阻塞）。

## 派发：DSH-2.1 + DSH-2.2 → Trae（2026-09-09）

- 老大拍板开干（原话：talk is cheap，发展的问题用发展解决，没有 100% 准备好再动手的）。**先派第一步 = 2.1 + 2.2，不一次全派**。
- **2.2 定为短路点、先做**：官方 demo 跑不通 = 整个 DSH-2 停下重估，故明确要求「任一不能 → 立即停下上报，不自行找 workaround」。
- **环境规格（WB 定默认值，改动成本为零时可改）**：基线 `dsh-v0.1.2-rc.1`（装指定版不要 latest）／独立 `DSH_HOME=.dsh-home`（已加 .gitignore）／profile 名 `larry`／工程目录 `harness/`／包名前缀 `@larryagent/`。
- **未派 2.3**：依赖 2.1 成果，且 2.2 若失败 2.3 更不可能 → 留作第二步。

## Trae 交付 DSH-2.1 / 2.2（2026-09-09）+ WB 复验

- **结论：两个"能"**——① 官方 demo 完整会话跑通（demo-ptc exit 0，输出 `hello from dsh`）② 自做插件经 B1 挂载并被 cordis 实际加载（`[B1-PROBE] external bundle loaded by cordis (tag=v1)`）。
- **WB 独立复验（不采信转述）**：
  * **静态** ✅ 自跑 `dsh --profile larry --dump-config` → 见 `# == @larryagent/plugin-probe` / `- id: larry-probe` / `tag: v1`，且 `hmr` row `disabled: false`
  * **动态** ✅ 自跑 `dsh --profile larry --help` → 输出 `[B1-PROBE] external bundle loaded by cordis (tag=v1)`
  * **未独立复跑**：§4.1 完整会话（需真实 key 触发 LLM），采信 exit 0 + stdout + 会话产物
- **⭐ 零成本复验法（可复用）**：`dsh --profile larry --help` **会触发 cordis apply，不需要 LLM key** —— 以后验证"插件是否被加载"一律先用这条，零消耗。
- **两处修正既有结论（重要）**：
  1. **「Windows CLI 稳定 segfault」口径修正** —— npm 全局 `dsh@0.1.2-rc.1` 在 Windows **实测全部可用**（`plugin add` / `--dump-config` / `--help` / 完整会话均 exit 0）；**卡住的是源码入口（`bin.ts` + tsx）在 PowerShell 下偶发**。→ 第 0 项记的是"首次触发条件"未验"根因成立条件"。**默认走 npm 全局入口**。
  2. **`patchReload` 是 `live` 不是 `startup`** —— 第 0 项判的 startup 出自 **sdk-app** bundle；我们实际用的 `larry` profile（`dsh-base` + `dsh-headless`）manifest 为 **`patchReload: live`**（WB `cat .dsh-home/profiles/larry/package.json` 核实）。**配置热载默认已开、不需要 hmr 插件**（模块级代码 HMR 是另一开关，已验证可开但需长驻 profile 观察）。
- **环境硬前提（后续阶段都要用，已进报告 §5.2）**：源码树跑 demo **必须先 `build:lib:host`**；`pnpm install --ignore-scripts`；**pnpm 11 `allowBuilds` 待审批项须全设 `false`**（否则 exit 1，别当成 dsh 坏了）；worktree 用法 `git -C ref/dsh-bare worktree add --detach D:/Code/dsh-src <tag>`。
- **落位**：报告由 `exchange/` 迁入 `docs/dsh/dsh-b1-plugin-probe-trae.md`（含环境复现步骤，后续仍被引用 → 按判据属证据区）。`docs/dsh/README.md` 报告数 4→5。
- **TODO**：DSH-2.1 / 2.2 全部勾选完成。
- **⚠️ 老大裁决**：key 进交流区一事，**不必纠结安全性与扩散、不必清洗残留，由老大做外部管控与适时关闭** → WB 不再处理该议题。

## 老大问：2.1/2.2 做完后，决策稿表述与 TODO 计划有无调整必要（WB 复检）

**结论：有必要，且这是「边做边研究边验证」模式的必然产物——计划追不上事实。** 已改 6 处，另留 2 处待拍。

**已改（事实性纠错 + 新事实沉淀，零争议）**：
1. TODO DSH-2.0 `patchReload: startup` → **live**（过期避坑，留着会误导）
2. TODO DSH-2.2「Windows dsh.exe segfault 走编程入口」→ **推翻**（npm 全局 CLI 全可用，卡的是源码 tsx 入口）
3. TODO 顶部状态「2.1–2.6 待派发」→ 2.0/2.1/2.2 ✅、2.3 已派
4. 决策稿 §3.5 + 承接总表 2.7.2：B1 从「他人实测可行」升级为 **🟢 我方工程自验**（plugin-probe 被 cordis 实际加载）——性质变化，不是措辞变化
5. 决策稿 §3.6 DSH-2 新增**环境规格定案表**（harness/ · @larryagent/ · profile larry · .dsh-home · npm 全局入口 · dsh-src）+ **警告：manifest 因 bundle 而异，结论不可跨 profile 外推**（第 0 项 patchReload 误判的根因）
6. 决策稿 §3.6 新增**零成本复验法**：`dsh --profile larry --help` 即触发 cordis apply、不需要 key

**待老大拍（未动）**：
- **前端通信面张力**（本次新发现、最实质）：§3.6 写死「走 sdk / acp」，但 **sdk 面只有 initialize/session.prompt/shutdown = 判二等的同一窄面** → client 长期走它则被锁在窄面，与 A-framework「贴近核心层」初衷打架。已在决策稿加 ⚠️ 注标明「2.3 实测后可能修订」，**未改结论**。
- **退出条件③ 权重**：Windows `ctx.sandbox` 与 ①②④⑤ 同处全局 go/no-go 闸门，按老大「S 侧是根基」定调，端侧局部项不该能触发整条路线重估。（老大此前回「无所谓、别纠结」，故未强推，但文档矛盾仍在）

**方法论沉淀**：边做边验证模式下，**每个阶段收口时应回扫一次「上一批结论里有哪些被本阶段事实推翻」**——本次一次扫出 2 条硬错误（segfault、patchReload），均是「第 0 项记的是首次触发条件、未验根因成立条件」的同类错误。与 MEMORY §七「'发生过'≠'只在那种条件下发生'」同源，第二次命中。

**老大补充（同日）**：决策稿与 TODO 是**一体两面**——决策稿 =「做什么」（事实/想法/验收基准），TODO =「怎么做」（执行），**两者同步，不必分开纠结**。→ 已写入 MEMORY §五：**改一面必同步另一面，两面不一致即为缺陷**；同步是双向的（定调下行 + 实测回流）。
- 按此原则反查出**一处理论上已一致、实际不同步**：前端通信面——决策稿原写「（DSH-2 定死）走 sdk / acp」，而 2.3 派发稿刻意留活口 → **同一件事两面口径不一**。已改为「保留 Vue/Tauri、不采用 Web-GUI」定死 +「通信面暂取 sdk/acp，待 2.3 定型」，并在注里加「TODO 侧与该判断保持同步、不先行按 sdk 面设计」。
