# 2026-08-12

## 加载 software-architect 专家 + 宏观架构 review

- 用户加载了"软件架构师"专家（软件架构专家），要求宏观 review 项目。已按架构师视角输出：容器图（对话/记忆/工具三子域 + chat_service 编排）、领域观察、权衡与演化建议。
- 确认：昨晚回归 bug 已修复并提交（`8575288 feat: P3.1 SSE streaming + fix archiver.py chat_completion regression`），工作区无代码改动。

### 架构师视角的核心判断（摘要）

- 当前是模块化单体，选型正确；处于"技术验证完成 → 产品化"转折点（P4/P5 前），最易引入过度设计。
- 三个领域观察：
  1. 边界侵蚀：tool_calls/tool_call_id 已写入 messages 表，对话历史被工具噪音污染（影响记忆摘要质量）。
  2. 领域断层：产品愿景多场景（roles），数据模型单域（长期记忆全库检索，source_role 未真正加权）。
  3. 双写一致性：靠周期校验兜底即可，无需分布式事务。
- 记忆隔离两条路：软隔离（检索加权，推荐，可逆）vs 硬隔离（每角色独立 collection，与跨域愿景矛盾）。
- 挑战的假设：P4 Vue 3 必要性存疑（现有 chat.html 够用）；意图识别应排在记忆隔离之后；单人项目扩展对象是记忆规模与工具种类，非并发。

## 协作模式确认

- 项目共 4 个 AI 角色参与 + 老大本人作为**人类调度者**，实时协调各 AI 的同步。老大知晓工程上"同步应交给机制（CI/契约）"的标准答案，但明确表示**乐在其中、享受灵活操作空间**，短期内不打算自动化这部分。
- 对 AI 的含义：老大在环（human-in-the-loop），各 AI 产出以 TODO/README/测试为契约对接；不必建议自动化调度工具，除非老大主动提出。

## .workbuddy 入 gitignore（已执行）

- 经确认，`.workbuddy/`（工作区记忆 + 项目技能）已加入 `.gitignore`（第 29 行），不入库。理由：本机运行数据、含项目安全/个人决策信息、高频变更噪音。换机时记忆不随仓库走，属可接受代价（可从对话重建）。

## TODO 新增 AI 交流讨论区机制

- 老大在 TODO.md 新增"AI交流讨论区"（各 AI 专属区域）+ "人类区域"（参与 AI 列表：WorkBuddy/Marvis/Claude Code/Trae CN/Qoder Work + 6 条原则）。
- Claude Code 与 Trae CN 已就 token 翻倍问题各写分析（结论一致：当前不修；建议分叉：触发再评估 vs 归入 P3.3）。
- 我（WorkBuddy 组长）已在自己区域写入管控记录：①token 翻倍裁决（采纳 Trae CN 方案 A 归入 P3.3，补 mock 跨 chunk 测试风险控制）；②三条架构观察（记忆隔离/边界侵蚀/上云前置）；③P3 推进顺序建议（P3.3→P3.4→P3.5→P3.2）。
- 给机制的架构建议：缺"决策收敛"步骤（AI 区=分析区，TODO 勾选=决策区，分歧由人类裁决）；"各自区域只能自己改"靠 git 追溯，约束力有限但够用。

## Marvis/老大建议落地（已更新 TODO 路线图）

- Marvis 宏观视角确认：P3 顺序（P3.3→P3.4→P3.5→P3.2）；全程流式唯一风险=跨 chunk 断裂，测试覆盖率作交付标准；架构债记账不追债（P5 前统一清算）；原则对照通过。
- 我执行的 TODO 调整：
  1. P3 标题下标注执行顺序（已确认）；
  2. P3.3 并入 token 翻倍优化（方案 A 全程流式）+ 风险控制（mock 跨 chunk 测试，覆盖率作交付标准）；
  3. P5 新增"上云前架构债清算"小节（记忆隔离/边界侵蚀/shell 白名单三条）；
  4. 我的交流区"P3 推进顺序"更新为已确认。
- 补充：WorkBuddy 定位写入项目 MEMORY.md（架构师+全局协调+技术分歧拍板，纯技术团队氛围，不逼进度）。

## P3.3 完成评估（组长独立验证）

- Claude Code / Trae CN 报告 P3.3 完成（16/16），TODO 已勾选。我独立验证（trust but verify）：
  - 代码实测通过：`chat_completion_stream_events` 全程流式 + `stream_tool_call_accumulator` 纯函数状态机 + `token_counter.py`，实现与规格一致；`test_chat_service.py` 16 项全过（含 call_count==1 断言证明消除双调用）。
  - 两 AI 报告与代码一致，未见虚报。
- **发现三个非 P3.3 问题**：
  1. 存量测试债务：`test_chromadb_degradation.py` 6 项失败（mock.patch 了 archiver 不存在的 get_db，重构后遗留，早已红无人发现）；
  2. 环境适配：integration 需 pytest-asyncio；test_windows_dir 中文 Windows dir 编码断言不兼容；
  3. P3.3 改动未提交（工作区脏）+ CLAUDE.md 未跟踪（Claude Code 项目说明，无敏感信息，归属待裁决）。
- 已替换 TODO 交流区 WorkBuddy 区域为评估记录；下一步 P3.4。

## 提交流程约定（已落地）

- 老大确认新提交流程：**提交是工作记录，验收是检查，两者分离**。每个 AI 完成可验证工作单元后立即 commit（feat/fix/test/refactor 分类），验收链不变（实现→测试→组长确认→人类确认→阶段完成），但验收对象是已提交代码；工作区不残留未提交改动。
- 已写入：TODO 交流区 WorkBuddy 区域 + CLAUDE.md（行为准则）。

## 配置 .claude/CLAUDE.md 与 .trae/TRAE.md（强约束配置）

- 老大将 `.claude/CLAUDE.md` 与 `.trae/TRAE.md` 定为配置文件（更强约束力），授权我按需配置。已重写两份（统一约束框架 + 角色差异化）：
  - 统一新增：提交流程约定（TRAE.md 原缺）；安全硬约束（config.yaml 的 key 不得复述到任何输出）；测试环境说明（pytest-asyncio 缺失、test_windows_dir 编码、chromadb_degradation 存量问题）；当前阶段更新为 P3.4 下一步；"架构问题与组长对齐"。
  - 修正：TRAE.md 的 archiver 路径错误（services/ → memory/）；测试命令改 pytest；可修改文件清单补 token_counter.py。
  - 保留：TRAE.md 的"可修改/不应修改"分工清单（加"互验优先于文件隔离"说明）；两份文件项目结构/原则/交流区机制。
- 澄清（老大）：`.claude/CLAUDE.md` 即从根目录 CLAUDE.md 移动而来，根目录已无该文件；`.claude/` 与 `.trae/` 是各 AI 工具约定的配置位置，在各工具允许范畴内形式相对统一。不存在重复文件问题。

## P3.3 完工提交（已执行）

- 按新提交流程约定拆三个 commit，工作区干净：
  - `fa42636 feat(P3.3): Token 统计 + 翻倍优化（全程流式）`（llm.py/chat_service.py/token_counter.py/tests/requirements.txt）
  - `49a3dd9 docs: P3.3 完成勾选 + AI 交流区机制 + 架构债清单`（TODO.md）
  - `dcbd4e9 chore: 忽略 .workbuddy + 新增 AI 配置文件`（.gitignore/.claude/.trae）

## 交流区从 TODO.md 迁移至 exchange/ 独立文件（已执行，commit 4c1b553）

- 起因：老大观察到各 AI 每次改 TODO.md 都要定位专属区域，token 消耗略大。评估后赞同迁移（token 量级性下降 + 消除多 AI 并行改同一文件的 git 冲突 + 契合关注点分离哲学）。
- 老大拍板：人类区域留 TODO.md，根目录建 `exchange/` 夹子分文件。
- 执行：`exchange/` 下建 README.md（索引+协作规则）+ exchange/log-workbuddy.md / exchange/log-claude.md / exchange/log-trae.md / exchange/log-marvis.md（从 TODO.md 277-388 行搬出）；TODO.md 删除四个 AI 区域，保留人类治理区（279 起），新增指向 exchange/ 的指针（277 行）。
- 顺带发现：迁移时确认人类区域第 282 行已更新为"Hy3免费时会由用户手动切换到Hy3"——老大已自行修正此前我暴露的"文档漂移"矛盾，符合"暴露矛盾→人类决策更新"机制；因"人类区域勿动"原则我未改动它。
- commit `4c1b553 docs: AI 交流区迁移至 exchange/ 独立文件`（6 files changed, +127/-110）。

## P3.4 计划确认与任务派发（已执行，commit 399500b）

- 老大要求确认 P3.4 计划并安排。以架构师视角核实：三个 API 路由前缀均为 `/api/*`（chat/memory/tools），中间件 `startswith("/api/")` 匹配正确；原 TODO 计划漏了 `config.py::ServerConfig` 缺 `api_key` 字段 + `config.example.yaml` 缺失，已补进子清单。
- 暴露点（矛盾暴露原则）：空 key 必须完全透传是最高优先级陷阱（写错则本机全站 401）；放行路径天然不被 `/api/` 命中，勿写冗余白名单；与 P5 硬耦合（改 host 0.0.0.0 前须先 set api_key）。
- 派发：Trae 实现 / Claude 测试 / WorkBuddy 确认 / 老大确认 → 勾选。规格写入 `exchange/log-trae.md`（Trae inbox）+ TODO 7 项子清单 + `exchange/log-workbuddy.md` 记录。
- commit `399500b docs(P3.4): 任务派发与验收规格`（3 files, +45/-6）。
- 注：WorkBuddy 作为组长只做派发/协调/确认，不亲自实现（实现归 Trae），符合既定分工。
