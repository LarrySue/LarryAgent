# 2026-09-06

## 15:50 启动 DSH 迁移讨论（定位级决策严肃讨论起点）

### 事实校准（WB 承认事实盲点）
- **DSH = DeepSeek harness**（老大 2026-09-06 15:50 砸实）
- **上次"不要迁"的具体理由已失考**：grep 全库无命中；08-15 那次交流区清理时一并清掉了三周前的讨论。老大当时也没追问，所以那次讨论极浅，我当时的"不要迁"判断很可能是凭印象、非结构化对比
- **无"延续上次结论"这回事**——旧论据已死，要不要迁只能从事实重新评

### 老大当前姿态
- 想做迁移、需要严肃讨论迁移成本
- 上次答"不要迁"未进一步追问——说明没阻力认知，只是没下决心

### 待砸实才能展开讨论的最小事实集（WB 暴露矛盾 / 不替老大编动机）
1. **DSH 是哪个具体项目**：仓库地址 / 定位（agent harness？LLM runtime？IDE？第三方 fork？）——决定能提供什么、绑定多深
2. **核心诉求**：用 DSH 自带的 LLM+工具调度？想借工具/记忆生态自己跑 LLM？还是看中了某个具体能力（多 agent 编排 / 评测 / 训练）？
3. **当前 LarryAgent 能力盘点 vs DSH**：FastAPI 后端 + 工具框架（BaseTool/SSRF/降级）+ 多角色 + 记忆（SQLite+ChromaDB+本地 Embedding）+ SSE + Vue/Tauri——缺什么、缺的是 DSH 能补的还是 DSH 也补不了

### WB 倾向（暴露不拍板）
- **agent harness 类**：核心问题是与自写工具框架/记忆层重叠度（重叠 80% = 搬家不是迁移）
- **LLM runtime 类**：讨论本质 = 自托管 vs 云 API（与既有讨论一脉相承）
- **IDE/编辑器类**：迁移无意义（Tauri 客户端已有）

### 15:57 已开讨论稿 `exchange/dsh-migration.md`

- **DSH 事实（2026-09-06 WebSearch 核实）**：DeepSeek 官方开源 **Agent Harness**（`Agent = Model + Harness`），**不是大模型/推理引擎**；定位**编码 Agent**（对标 Claude Code/OpenHands）；Cordis 插件内核 + **TypeScript** Monorepo（40+ 包）；MIT；**2026-08-13 创建、当前 0.1.0-rc.7、官方明示 developer preview 会破坏性变更**；四模式 standard/PTC/minimal/creative；亮点 = Trajectory 轨迹持久化（暂停/分叉/回放/断点续跑）+ 权限沙箱人工审批 + 插件总线 + 增量 diff 改文件
- **上次「完全不适合也没有必要」拆两个判断检验**：
  - 「完全不适合」**维持**——编码 Agent vs 通用多角色对话助手，定位错配成立
  - 「没有必要」**翻一半**——DSH 的 Trajectory / 权限沙箱 / 插件总线是 LarryAgent 真没有且自造费工的能力；但这是**值得借鉴 ≠ 值得迁移**
- **关键概念歧义**：「迁移」四种理解成本差数量级——A 换底座（Python→TS 重写，最重）/ B 嵌一层（跨语言通信，中）/ C 借思路（纯增量无风险，轻）/ D 接能力（当外部能力调用，轻-中）
- **两条硬风险**：①语言栈冲突（DSH=TS vs LarryAgent 后端=Python FastAPI）②押注 3 周大 rc 框架的破坏性变更返工风险
- 待老大砸实核心诉求（4 选项）→ 才拆人天/风险/返工面

### 16:10 老大给功能边界八项 + 纠正 Qoder/DSH 认知，讨论稿大幅更新

**老大纠正（重要事实）**：
- **Qoder ≠ QoderWork**：Qoder（IDE/插件）= 专业编码；**QoderWork（桌面 AI 助理）= 通用知识工作**——起草文档/分析数据/控浏览器，内置产品·运营·HR·法务·财务·数据分析**角色技能库**，可自定义**专家套件**（无需编码）+ 安全沙箱。对标产品是 QoderWork（`HUMAN_NOTE.md`），**非编码 Agent**
- DSH 在老大理解中也非编码 Agent → WB 核实后**部分成立**：DSH **架构通用**（Cordis 无特权内核，连 Agent Loop 可换）、**开箱偏编码**（内置工具=文件/Shell/代码解释器，四模式围绕编码）。官方口径亦有"不是单一编码助手，是可组装智能体基础设施"的说法。故 §4.1「完全不适合」修正为**说过头**（另有两处事实：LarryAgent 5 角色**含 code**；对标 QoderWork 是通用办公）

**老大认定的功能边界八项**（元问题）：①多模型按需切换 ②工具 ③专家/角色 ④长期+短期记忆 ⑤知识库 ⑥云端部署多端 ⑦用户画像 ⑧超长会话一致性

**八项 × 现状对照（WB 核对）**：①✅已有(config.yaml 任意增模型) ②✅已有(3 工具) ③⚠️部分(5 角色 default/code/health/finance/science，但"专家"=技能库/专家套件现无) ④✅已有 ⑤❌**无**(grep 零命中) ⑥⚠️部分(草案未实施) ⑦❌**无**(TODO 有条目未做) ⑧⚠️**弱且机制反向**(max_input_tokens 截断旧消息，**方向与目标相反**)
→ **三个真缺口：知识库 / 用户画像 / 超长会话一致性**

**八项 × DSH 供给对照（核心产出）**：
- 真增量只在两端：**#2 工具管道**（拦截/沙箱/审批/重试/规范化）、**#8 超长会话**（compaction 上下文压缩 + resume + fork + 回放，恰是 LarryAgent 方向性错误那项的现成正解）
- **#5 知识库、#7 用户画像 DSH 一概不管** → 迁移了照样从零做
- **#4 互补不替代**（关键，易误判）：DSH session log = 原始事件流水（回放/审计）；LarryAgent memory = LLM 摘要+向量（跨会话语义召回）。**迁移 ≠ 获得记忆能力**，换底座须重建 memory 层或双轨
- #1 纯重叠无增量
- 新增 DSH 事实：内置 5 模型适配器+多模型路由、**compaction**、子代理 spawn/fork、沙箱分级(workspace-write/read-only/danger-full-access)、**ACP 协议**（headless 外自动化服务，B/D 路径集成抓手）

### 下一步
- 等老大确认 §3 对照是否认同 + 八项边界有无遗漏 → 再定诉求类别(①②③④)
- §9 二次调研最高优先级：**DSH compaction 具体策略**（决定 #8 怎么抄）

### 16:50 功能边界定稿 13 项写入 README 开头（老大整合）

老大把功能边界整合为 **13 项**写入 `README.md` 开头（平铺列表）：
多模型按需切换 / 工具挂载 / 专家（或角色）切换和自动路由 / 长期记忆和短期记忆 / 用户画像 / 知识库 / 时间一致性 / 云端部署多端使用 / 使用成本管理 / 行为安全约束 / 沉淀信息可见 / AI行为可见 / 良好的超长会话一致性

**老大对 WB 补充八项的处理（采纳方式值得记）**：
- ①安全 → 采纳为「行为安全约束」；②成本 → 采纳为「使用成本管理」
- ⑤可观测性 → **拆成两项**：「沉淀信息可见」+「AI行为可见」（对象不同）
- ⑥时间感知 → 采纳为「时间一致性」；⑦意图识别与自动路由 → **合并进**专家项
- ③④⑧ → **判定为项目实施层面，非产品能力层**（⑧评估度量、③数据安全可恢复）

**WB 复验提 4 处问题**：①13 项混 5 层（能力/约束/可见性/质量/形态）平铺难维护 ②「云端部署多端使用」与下方「部署定位」段重复（是"在哪用"非"能干什么"）③「时间一致性」命名歧义（易读成跨会话时间线一致，实为时间上下文对齐，建议改「时间感知」）④「沉淀信息可见」可能写窄（HUMAN 待办原话是"如何管理"，是管理非仅可见）
→ 建议最小改动：加 5 个分组标题（能力/约束/可见性/质量/形态）+ 挪一项 + 改两词

**老大范围纠正（重要，防越界）**：引用记忆「两层区分」——当前只探讨**产品能力层**；项目实施层（数据安全可恢复、评估度量等）与协作机制层**先不聊**。WB 上一轮追问「③⑧ 落哪」属越界，撤回。

**当前仅剩两个待定（均属产品能力层）**：①会话管理要不要单列（TODO 有设置/归档/回收站三件套 + HUMAN 待办沙盒工作空间，13 项里无）②README 是否按分组方案改

### 16:55 功能边界定稿落 README（14 项五组）+ WB 补承接

**README 开头定稿**（老大改，WB 复验：13 项全部保留无遗漏 + 新增 1 项）：
- **能力**（7）：多模型按需切换 / 工具挂载 / 专家（或角色）切换和自动路由 / 长期记忆和短期记忆 / **会话为单元管理**（新增）/ 用户画像 / 知识库
- **约束**（2）：使用成本管理 / 行为安全约束
- **可见性**（2）：沉淀信息可见（可管理）/ AI 行为可见
- **质量**（2）：时间感知（上下文对齐）/ 良好的超长会话一致性
- **形态**（1）：云端部署、多端使用（交叉引用下方「部署定位」段）
- 措辞修正已采纳：时间一致性→**时间感知（上下文对齐）**、沉淀信息可见→**（可管理）**

**老大最终采纳会话项**：措辞定为「会话为单元管理」——WB 复验指出"会话管理"装了两件事（UI 列表 vs 作用域容器），老大认可"作用域/单元"角度后列入。判断标准记录：若将来认定"会话应有独立作用域/沙盒"是产品能力，则属此列；现仅 UI 层则不列。

**WB 补两处承接**（防两份真相）：
1. `dsh-migration.md` §0.1 加「权威版本见 README 开头（定稿 14 项）」，本节降级为过程记录
2. §3 对照表加警示：**基于初版八项**，新增 6 项未纳入；其中**「行为安全约束」（DSH 沙箱分级审批）与「AI 行为可见」（DSH Trajectory 轨迹）大概率另有增量** → 原表对 DSH 的增量评估**偏保守**，待补

### 跨 session 上下文要点（防未来 AI 误读）
- 这条讨论起点是 2026-09-06 15:50 老大主动发起
- WB 上次（08-15 前后）反对迁移的具体论据**已不可考**——新接手的 AI 不要试图"延续上次立场"，按事实重新评
- 老大说"做严肃讨论" = 要结构化展开，不是 yes/no 速答