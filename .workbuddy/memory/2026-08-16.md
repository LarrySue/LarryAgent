# 2026-08-16 工作日志

## 乱中乱复盘审计（00:00）
对 P4 第二波事故链（git 历史脱敏 + Claude 污染真实库）做现状审计，发现上一轮"61 提交零锚点/远程验证通过"结论不成立：

- **严重**：git 历史仍含社交账户锚点 token（Discord 判别码、Discord/Twitter/微博/NGA 引用及旧项目名），全部位于 `.workbuddy/memory/2026-08-15.md`，横跨约 9 个提交。根因：该 memory 文件是 WB 自己在会话中写的每日记忆，复述了旧项目审计内容；上一轮 git-filter-repo 只覆盖 exchange 日志，漏掉 memory 文件，验证覆盖不全。本地+远程双端均污染（origin/master 已含相关提交）。`.workbuddy` 未进 .gitignore。
- **中等**：db 路径为相对路径 `data/larry.db`，按启动目录不同写出两个内容不同的库（根目录 `data/larry.db` id 1–27；`backend/data/larry.db` id 226–252）。`.gitignore` 只忽略 `backend/data/*.db`，未忽略根库。真实库污染清理本身生效（backend 库已无 253–266 那段）。
- **待办**：`larry.db.bak_preclean` 仍留存盘（含 13 条污染会话），按"本地清理干净"指令应删；本地 ahead 10 未推送，且**此刻不能原样推送**。

## 建议（待用户拍板，未执行）
A. git-filter-repo --replace-text 全历史抹掉社交 token + 清工作树 memory 文件 + force push 覆盖远程；
B. db 路径改绝对锚定 + 补 .gitignore；C. 删 bak_preclean；D. 脱敏后全文件 grep 验证再推送。
用户尚未授权执行 A（破坏性历史重写）。

## 用户决策：放弃历史二次脱敏（00:17）
用户判定残留可接受、不再重写历史：裸 Discord 判别码 3705 几乎不算锚点；仅提社交平台名、不涉及账号与连接则风险低；且真正敏感项（完整手柄 LarrySu#3705、twitter.com/weibo.com 链接）上一轮已刮掉。结论=残留为可接受残留，A 方案作废，不再 force push。
→ 此前"禁止原样推送 10 提交"的约束按用户标准解除（推送只是把已在 GitHub 的内容再传一遍，无新增暴露）。
→ 双库/relative db 路径（B）与 bak_preclean 清理（C）为独立议题，未决，留待另聊。

## bak 与双库清理（00:21）
用户授权：删 bak_preclean；两个库无必要、删一个（任意）。执行：`rm -f data/larry.db backend/data/larry.db.bak_preclean`，保留 `backend/data/larry.db`（Tauri 生产从 backend/ 启动会用到）。
- 核实：两个库各自 27 会话/207 消息（非真空，但均为开发期测试垃圾：根库 id 1–27 上午空标题会话；backend 库 id 226–252 "loop forever/test persist/call ghost" 等）；bak 40 会话/211 消息（含 13 条污染会话）。三者均已被 .gitignore 覆盖，无入库风险。
- 现状：磁盘现仅余单一 `backend/data/larry.db`，git 状态干净。

## 测试环境隔离决策（架构问答，待用户拍板）
用户问：是否要建测试库/独立测试环境、与生产隔离是否过度复杂、同环境是否 OK。
- 结论：现阶段**不建独立测试环境、不建持久测试库**（对个人项目属过度工程，符合逃熊最小动作）。
- 关键红线：**测试绝不可读写生产数据文件**；正确做法是测试一律用临时/隔离 DB（经 `LARRY_CONFIG` 指向 tempfile 或内存），即用即弃。Claude 修复版测试已用"三保险隔离"验证 17/17 零二次污染，将此定为项目约定即可。
- 持久测试库反而是坏主意（又多一个会散落/误提交的 db 文件，一如刚删的那两个）。
- 真正需硬隔离的时机：真实对话/记忆灌入后想跑回归测试且怕动到它们时，届时也只需测试配置+临时 DB，仍不需 Docker/独立服务器。
- **路径锚定修复已执行（00:25）**：用户授权"yaml 里改一下"。实施：在 `backend/config.py` 的 `load_config` 中，将 `database.path`/`vector_store.path` 的相对路径一律相对【配置文件所在目录】解析（`Path(config_path).resolve().parent`），不再依赖 CWD；`config.yaml` 注释同步更新为准确描述。验证：从项目根与 backend/ 两种 CWD 均锚定到 `backend/data/larry.db`；测试用绝对路径 DB 原样透传（test_conversations.py 17/17 已证隔离）。双库问题彻底消除。该改动为代码变更，待随下次提交入版本库。

## 交流区报告收口 + WB 自我纠正（00:45）
将今晚复盘写成 `exchange/report-2026-08-16.md`，Claude/Trae/Marvis 各附看法，三方共识=三起事故同根"验证范围<影响范围"。
- **Claude 指出 WB 的小车（已核实属实）**：P4.6 复验时 WB 把"37/37 全绿"当"行为正确"，未确认 `test_exceptions.py` 旧断言是否钉住新行为。旧断言只查 `status==500`+响应不含 `traceback`/`valueerror`，而 Starlette 默认 500 也满足 → 即使兜底 handler 没生效测试也绿（虚假信心）。Claude 已用 `19dad0b` 补 3 项断言（统一 JSON/不泄漏/服务端留 traceback），11/11 全过。
- WB 在报告补"收口共识"段：承认自身也踩同一条病根；采纳"影响面清单"作为复验流程；config.py 修复仍待提交（工作区漂着）。
- 报告即日起锁死不再改。修正了 workbuddy.md 中"历史已脱敏"旧误记。
