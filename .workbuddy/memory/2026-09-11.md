# 2026-09-11 工作日志

## git rm 现象排查收尾

- **定调（老大）**：现象奇怪，不再纠结；**今后一律不做 `git rm` 操作**（需求提出后由老大亲自执行），日常删除走 `rm` + `git add -u`。
- **约束落位**：老大已把相关环境记忆/约束**移到全局层**（提交 `3024708`），项目侧不重复写，避免两处打架。
- **测试物料清理完毕**：`docs-test-wb` / `something-test-wb` / `archive-test` / `docs-test` / `gitemtest-3` 磁盘均不存在，`git status` 干净，无悬空。
- 执行记录（老大授权下的对照）：WB 工具侧 `git rm` 三次（`docs-test-wb/dsh/dsh-pysdk-probe-trae.md`、`docs-test-wb/product-positioning.md`、`something-test-wb/something/2026-09-10.md`），均 exit=0，stdout 只报被点名文件；stderr 伴随 `shell-runtime-bash-env.sh` 的 `dirname: command not found` / `cd: null directory`（工具层 shim 噪音）。具体磁盘后果由老大观察，本轮不展开。

## docs/dsh 报告"吸收再删"（第 2 份：B1 挂载报告）

- **对象**：`docs/dsh/dsh-b1-plugin-probe-trae.md`（DSH-2.1/2.2 短路点实测，Trae）。老大指令：**一份一份来**，先做这份，吸收完告诉他、由他删原件。
- **落位决定**：README 规则 1 明说"DSH-2 的**环境复现步骤**以 B1 报告为依据" → 归宿 = `docs/dsh/dsh-local-env.md`（本机环境唯一真相源）。
- **新增 §8**「DSH 工程搭建与短路点复跑」（+97 行）：两个"能"结论 + WB 复验/未复跑边界、8.1 版本基线（Node v24.14.1 / pnpm 11.7.0 / dsh 0.1.2-rc.1）、8.2 自做插件 bundle 声明范式、8.3 复跑步骤（源码 demo + B1 挂载）、8.4 六条踩坑、8.5 模块级 HMR 开关、8.6 源码工作树生命周期。
- **引用更新**：`docs/dsh/README.md` 删索引条目 + 规则 1 改指向 §8（`dsh-local-env.md` 索引描述同步补 §8）。经 grep 确认全局仅此一处硬引用；决策稿里的"§3"指向 Claude 的 pysdk 报告，非本件，删后无死链。
- **状态**：改动未提交（`M docs/dsh/README.md` `M docs/dsh/dsh-local-env.md`），**原件已由老大删除**（staged `D`）。

## docs/dsh 报告"吸收再删"（第 3 份：DSH-2.3 连通报告）

- **对象**：`docs/dsh/dsh-23-vue-tauri-connect-trae.md`（DSH-2.3 Vue/Tauri ↔ DSH 连通，sdk profile + TS SDK，判"能"）。
- **落位（两处，按内容性质分）**：
  - **能力边界观察（能做/做不了）** → 决策稿新增小节「**sdk 面实测能力边界**」（紧接 B 段 Gateway 判定之后）。依据：B 段已定为 **SDK（stdio）**，故该表即 **B 段的能力清单**；同时是决策稿「未收敛项」（sdk 窄面）的实测答案。含模型 id 改名注记（`deepseek-v4-flash` → `deepseek-flash`，官方 catalog 未跟上 → 必须真实调用冒烟）。
  - **连通方式 / 复跑步骤 / 踩坑 / GUI 点验** → `docs/dsh/dsh-local-env.md` 新增 **§9**（+154 行）。
- **引用更新（3 处，含 1 处代码引用）**：
  1. `docs/dsh/README.md`：删索引条目；规则 1「通信面选型以 2.3 报告边界观察为输入」→ 改指决策稿「sdk 面实测能力边界」+ local-env §9；local-env 索引描述补 §9。
  2. `TODO.md:114`：报告指针 → 改为指向两处落位。
  3. **`harness/tests/real-api.ts:271`**（代码注释里的建-profile 指引）：`dsh-23-vue-tauri-connect-trae.md §4.1` → `dsh-local-env.md §9.2`（**不改则留死引用**）。
- **过时表述修正**：决策稿 §313「待 DSH-2.3 实测后定型」、§315「未收敛项（待实测后定型）」→ 补"已实测"指针（与 §316 定型结论不冲突）。
- **死链检查**：全仓 grep，仅剩两处"原文件已删"的留痕说明，无死链。
- **状态**：原件未删，**等老大执行**；改动未提交。

## ⚠️ 工具层教训：同一文件的并行 Edit 会互相覆盖

- **现象**：对**同一个文件**发起多个 `Edit` 调用（同一消息内并行），返回值均报 success，但**只有最后一个落盘**，其余静默丢失。本次实测：README 的三个并行 Edit，仅第 3 个生效；migration 的两个并行 Edit，仅第 2 个生效。
- **成因**：并行 Edit 基于同一基线快照，后写整文件覆盖前写。
- **纪律**：**同一文件的多个 Edit 必须串行**（一次一个，等返回后再下一个）。跨不同文件的 Edit 可并行。
- **验证手段**：改完后**必须 Read 磁盘复验**，不能只信 Edit 的 success 返回。

## T2 触发线待验：官方 web surface 经反向代理对外是否可行（WB 实做，2026-09-11）

- **背景**：TODO「⭐ 通信面定型」段待验项 T2 —— 原设「`--trusted-host` 白名单为唯一已知障碍，未实测反代」。老大问派发，判为 WB 做（产出=架构触发线判定，属架构角色）。
- **机制确认（`dsh web --help`）**：`/api` 上有 **browser-trust fence**；`--trusted-host <authority...>` = fence **额外接受**的 authority（host 或 host:port，可重复）。表面障碍 = Host 校验，非端口/网卡。
- **实测结论（本机，dsh 0.1.2-rc.1，`web` profile 已存在）——反代可行**：
  | 场景 | 结果 | 读法 |
  |---|---|---|
  | root 无 token | **401** | token 门 |
  | 直连 Host=回环 `127.0.0.1:8799` | **404** | fence 放行（路由无此端点）|
  | 直连 Host=`evil.example.com` | **403** | **fence 拦** |
  | 反代·Host 重写为回环 | **401**（≠403）| **fence 放行 → 反代有效** |
  | 反代·Host 原样透传 | **403** | 仅透传无用（对照）|
  | 服务加 `--trusted-host evil.example.com`：直连 Host=evil | **401**（≠403）| **官方口子生效** |
  | 同上 + 反代透传 | **401**（≠403）| 官方口子对反代同样生效 |
- **→ 两条路都通**：① `--trusted-host` 显式放行；② 反代时把 Host 重写为回环（社区普遍做法）。**"唯一已知障碍"实为可绕过的 Host 校验，不是真阻断。**
- **旁证（非实测，采信留边界）**：`ref/tmp_awesome.md.bak` 里已有**数十个**独立第三方反代/网关插件，明确写"Host/Origin rewrite through the loopback trust fence, WebSocket upgrades included"、"no --trusted-host needed" 等 → "可行"与"省事"两头均有社区实作支撑。
- **本轮未覆盖**：WebSocket 升级经反代（只测了 HTTP `/api`）；完整 token→cookie 登录流（cookie 未取到，故 401 vs 404 的路由差异未完全归因）；`--trusted-host` 的**安全性**（未评估）—— 只测了"通不通"，未测"安不安全"。
- **探针物料**：`D:\Code\t2probe\`（`proxy.py` + `run.sh` / `run2.sh`，仓库外，未污染 git）。无残留进程。
- **待老大**：① T2 命中与否的判定（"可行"已证，"省事"需与中转比成本）；② 结论落位去向（`dsh-cloud-deployment.md` §7.1 还是 `dsh-migration.md` 触发线处）。
- **老大裁决（2026-09-11）**：**T2 认定为「命中」**。TODO 已同步 —— 原 `[ ]` 行改写为 `[x] T2 触发线 → 判定「命中」`，按实测结果重写内容（两条通路 + 七格证据 + 「命中 ≠ 推翻定型」+ 未覆盖边界），物料指向 `D:\Code\t2probe\`。**其他未动**（决策稿 T1/T2/T3 触发线处的落位待后议）。

## DSH-2.1 / 2.2 挂 ✅（2026-09-11）

- **2.1 配套 TS 工程 → ✅**：实物核实（非采信声明）—— `harness/pnpm-workspace.yaml`、`tsconfig.json`、`packages/plugin-probe/src/index.ts`（`export const name/inject` + `apply(ctx,config)`）、`package.json` 含 `dsh.bundle.patch`、`cordis.patch.yml`（`id: larry-probe`）；且 **WB 独立复验过 B1 挂载**（打出 `[B1-PROBE] external bundle loaded by cordis`）。
- **2.2 跑通官方 demo → ✅（附证据边界）**：交付成立，但那条"完整会话"是**交付方证据**（Trae 报告 exit 0 + 会话产物），**WB 未独立复跑**（需真实 key）。已在回复中标出，供老大知情。
- **状态**：TODO 改动未提交（与 T2 同批）。

## TODO 过期条目同步（2026-09-11，三处）

- **判据**：老大问"2.4 最后一个没勾的是不是派给 Trae 的" → 查证：**不是**，2.4 那条 `--real-api` 等价物**派的是 Claude**且**已交付**（`0e2a7e9`，WB 复验通过），属"两处维护、一面没同步"。派给 Trae 未回的是 **2.5③「二轮收尾」R1/R2/R3**（`TODO:179`/`221`，规格在 `exchange/log-trae.md`）。
- **改动（三处，串行 Edit）**：
  1. `TODO:157` → 勾掉 + 指针：已由 Claude 交付（`0e2a7e9`）+ WB 复验通过；退回件按偶发收口；执行维护走 DSH-3「前置件 1」。
  2. `TODO:214`（收口清单 T2 行）→ `⬜ C 低优先/未实测` → `✅ 已命中（判毕）`，指向通信面段 T2 条目。
  3. `TODO:212`（收口清单 A 行）→ `🔄 退回 1 项` 补为「**已收口**」（老大裁定偶发 + 20 分钟墙钟看门狗兜住）。
- **治理检查**：`awk` 扫 `✅` 小节含 `[ ]` → **无冲突**；DSH-2.4 段内 `[ ]` → **0 个**（段已全勾）。
- **状态**：TODO 改动未提交（与 T2 同批，共 10 insertions / 7 deletions）。

## TODO DSH-2.0–2.5 精简 + docs/dsh 同步（2026-09-11，老大指令）

- **指令**：TODO 里 DSH-2.0–2.5 精简为"结论 + 状态为主"，减少反复性过程记录；并按已完成情况同步修改 `docs/dsh/` 对应内容。
- **执行顺序**：先 docs 承接 → 再删 TODO → 逐条复验（治理纪律：删项必须有承接）。
- **docs 承接（6 处，先落位）**：
  1. `dsh-migration.md` §3.4 风险表两条"待实测"占位（会话存储外接 / ACP 契约）→ 挂 2.5 ①② 实测结论。
  2. `dsh-migration.md` DSH-2 退出条件 5 项 → 各挂实测结论（**5/5 全部通过**）。
  3. `dsh-migration.md` DSH-4 承载表「记忆迁移」行：原「**全量重嵌**」→ 按 2.5⑤ 实测改为「**无需全量重嵌**」（硬前提：预处理严格对齐）。**这是"决策稿 vs 实测"两面不一致的翻牌**。
  4. `dsh-cloud-deployment.md` §7.1 → 追加 T2 触发线实测（反代可行，含 6 行证据表 + 未覆盖边界）。
  5. `dsh-cloud-deployment.md` §8 → 补一行「~~embedding 是否需全量重嵌~~ ✅ 无需」。
  6. `archive/roadmap-history.md`「DSH-2.4」段 → 反向指针改向（原指 TODO，TODO 精简后失效），**复验结论 + 关键判据直接落本段**（本节成为该任务唯一事实源）；§5 补 `.dsh-home` 数据落点结构。
- **TODO 精简（9 处）**：2.0 / 2.1 / 2.2 / 2.3 / 通信面定型 / 2.4 / 2.5 各压成状态 + 指针；分区约定状态行更新（DSH-2 已收口）；DSH 迁移区标题更新；收口清单 T2 行指针改指 cloud §7.1。
- **docs/dsh/README.md 同步 2 处**：① local-env 判据描述**订正过期表述**（"无 `turn/end.reason`" → "`turn/end.reason.kind === 'completed'`"）；② cloud 索引描述补 T2 落位。
- **治理检查**：死链 grep → 干净（无旧指针残留）；`awk` 扫 `✅` 小节含 `[ ]` → 无冲突。
- **改动面**：`TODO.md`（159 行）/ `dsh-migration.md`（18）/ `dsh-cloud-deployment.md`（17）/ `archive/roadmap-history.md`（9）/ `docs/dsh/README.md`（4）。
- **未动**：决策稿 T1/T3 触发线处（T2 只落 cloud §7.1，未回填决策稿触发线列表——老大未指示）。

