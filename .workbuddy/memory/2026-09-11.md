# 2026-09-11 工作日志

## git rm 现象排查收尾

- **定调（老大）**：现象奇怪，不再纠结；**今后一律不做 `git rm` 操作**（需求提出后由老大亲自执行），日常删除走 `rm` + `git add -u`。
- **约束落位**：老大已把相关环境记忆/约束**移到全局层**（提交 `3024708`），项目侧不重复写，避免两处打架。
- **测试物料清理完毕**：`docs-test-wb` / `something-test-wb` / `archive-test` / `docs-test` / `gitemtest-3` 磁盘均不存在，`git status` 干净，无悬空。
- 执行记录（老大授权下的对照）：WB 工具侧 `git rm` 三次（`docs-test-wb/dsh/dsh-pysdk-probe-trae.md`、`docs-test-wb/product-positioning.md`、`something-test-wb/something/2026-09-10.md`），均 exit=0，stdout 只报被点名文件；stderr 伴随 `shell-runtime-bash-env.sh` 的 `dirname: command not found` / `cd: null directory`（工具层 shim 噪音）。具体磁盘后果由老大观察，本轮不展开。
