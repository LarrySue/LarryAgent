# 2026-09-08 工作日志

## DSH 决策稿定稿（老大终审）+ 阶段 2 第 0 项派发 Trae（commit 885f46b）

- **老大终审（2026-09-08）**：DSH 迁移讨论稿看了一遍，**§1.5 基准无问题 / §3.0 保持裁定无问题 / §3.7 计划基本合理（后续按实际推进微调）/ 唯一待定 = §3.5 须明确判定 Py SDK 成色**。→ 决策稿**已定稿**（第 3 轮四方审阅 + 老大终审两道关都过了）。
- **文档状态同步**（避免后来 AI 误判卡点）：头部状态改「已定稿（老大终审通过）」并写明终审结论；**§3.5 提示框首行「待老大拍板」→「不预先拍板，先由第 0 项实测判定」**（关键：否则后续 AI 会以为方向还卡在老大那里）；§3.7 第 0 项标「🔄 已派发 Trae」；删除重复的「跑通官方 demo」待办（已被第 0 项 A 步覆盖）；README 索引改「已定稿」；log-workbuddy 议题栈重写（定稿 / 在飞 / 挂件三段）。

### 派发稿（写在 `exchange/log-trae.md` 顶部）设计要点

- **唯一问题**：Python SDK 相对原生 TS 路径是一等公民吗？输出**只能三选一**（一等 / 二等 / 无法判定），「大概/倾向」不接受；判「无法判定」须写明缺哪条证据。
- **判定操作化四条**（WB 定，**注明可被 Trae 推翻并给替代口径——不要闷头执行自己认为错的判据**）：① 能力覆盖无实质缺口（判据 = 31 子项任一必需能力在 Py 侧拿不到即为缺口）② B3 MCP 桥可行 + B1|B2 至少一条 ③ 官方支持度三项占二（实测与文档冲突以实测为准）④ **无系统 Node 环境**可跑通。
- **两条硬约束**：① 验「无需系统 Node.js」**必须把 Node 移出 PATH**（机器上装着 Node 就等于没验，这条声明是我们不管 Node 依赖树的全部依据）② 跑 demo 的凭据**一律走环境变量，禁用 `config.yaml` 真实 key**，禁止进报告 / 日志 / 命令行回显 / git。
- **交付格式七块**（独立文件 `exchange/dsh-pysdk-probe.md`，WB 折入后清理）：环境事实 / A 结论+RPC 方法面清单 / B 三通道表 / 事件流粒度 / C 判定四条+最终判定 / 落地形态建议 / 意外发现与「你认为我判断错了的地方」。全部断言标 🟢🟡🔴。
- **明确「不做的事」**：不写实现代码（是测绘不是 prototype）／不引第三方插件（§3.0）／不改结论区（已定稿）／不 checkout DSH 源码（走 `ref/dsh-bare`）／venv 装项目外且不进 git。
- **完成标准**：老大拿到报告能直接拍板 A 的最终形态，不必再追问「所以到底是不是一等公民」。
- **在派发稿里点明上一轮复验结果**：Trae 报的四条发现我核实后三条属实、一条（win-x64 快照）不存在 → 明写「不要复述 README 原文当证据而不自己跑一遍」。

- **WB 后续动作**：Trae 交付后复验（不采信转述，本地锁定版核关键断言）→ 折入 §3.5/§3.7。若判二等 → 按老大裁定转全面贴近核心层（含语言）。
- **教训（自用）**：定稿后**「待 XX 拍板」这类状态标记必须同步翻转**——内容与拍板已完成但标记没改，会让后续 AI 把已闭环的事当成阻塞项，是比内容错误更隐蔽的失真。

## 第 0 项改三方并行独立执行（老大裁定「此事重大」，commit 76a3854）

- **老大指令**：同一任务同时派给 **Claude** 与 **log-other（编外，建议 Qoder）**，与 Trae 并行。
- **三方同题不同视角**（避免同质化重复劳动）：Trae = 全量主路径／**Claude = 边界契约层 6 类失败模式**（他自己上一轮提的，且他角色就是代码测试 AI，做能力测绘不越界）／**编外 = 反向举证 + 审查我那四条判定阈值**（另两方天然偏乐观，专派一方找"其实这是二等公民"的证据）。产出各自独立文件 `dsh-pysdk-probe.md` / `-claude.md` / `-other.md`。
- **⚠️ WB 发现的关键风险（三方同机跑）**：`dsh plugin --profile sdk add` 会把持久 patch 写进**全局** `$DSH_HOME/profiles/sdk/cordis.patch.yml`——三人共用同一 `DSH_HOME` 会**互相覆盖 patch，产出互相污染的结果**，且症状会伪装成"偶发失败"。已写入三方派发稿作为硬要求：各自独立 venv + **各自独立 `DSH_HOME`** + MCP 探针不同端口。
- **防锚定**：要求先各自独立跑完落盘、再交叉比对（WB 做），**不先看他人报告**——否则交叉验证退化为附议。
- **冲突收口**：**以「可复现实证」为准，不以多数票为准**；给不出可复跑步骤与原始输出的一方，结论降为 🔴 不作判定依据（沿用证据纪律：可验证性 > 数量 > 身份）。
- **已写入 MEMORY.md**「多方并行验证三规则」：环境隔离 / 防锚定 / 冲突以可复现实证为准 + 专派一方反向举证。
- **教训（自用）**：多人（多 AI）并行同一任务时，**第一件事是找共享可变全局状态**（配置目录、全局 patch、固定端口、共用 venv）——这类污染最阴险之处是**症状表现为偶发/不稳定失败**，会被误判为"框架 alpha 不稳定"而不是"我们自己互相踩"。

### 老大点名指派（commit 72ec021）：Trae 第一 / Claude 第二 / Qoder 第三

- **老大澄清**：**不存在接单，三方都是他口头点名派的**。→ log-other 去掉「自愿接单 / 建议 Qoder / Marvis 亦可接」，改为直接指派 Qoder；产出文件 `-other.md` → `-qoder.md`；三份派发稿状态行加顺位（**顺位即优先级**，资源/环境冲突时按此序让行）。
- **协作机制事实（重要）**：**真正的派发渠道是老大口头指令，exchange/ 交流区只是记录 + 规格细化**。故三份派发稿均加**指令来源声明**：任务由老大点名指派（口头），规格为 WB 细化；**若与收到的老大口头指令冲突，以老大口头为准并立即指出冲突点，不自行折中**。（防两种失真：① AI 把 WB 细化规格当老大原话 ② 老大口头与文档不一致时被静默消化。）
- **Qoder 第三顺位的时点设计**：他大概率跑得最晚、会先看到前两方结论 —— 不禁止，而是**要求先落盘自己的独立判定，再回头针对其结论做反向举证**（把顺序劣势转为优势，但不得先看再跑）。
- **教训（自用）**：老大的"点名顺序"要问清是**执行次序还是优先级**；本次按「上一轮明确说『同时』」判定为**并行 + 顺位=优先级**，已在文档写明，若老大本意是串行需改（串行则 Qoder 的防锚定规则要整体重设）。

### 老大澄清顺位本意 → 隔离方案升级为可验证（commit 19e29b3）

- **老大澄清**：点名顺序**不是优先级、也不是串行本意**，"只是觉得让他们分开执行可能能回避一定程度上的污染或者操作冲突"。→ 顺位重定义为「**资源冲突时的让行次序**」，三方并行不变；在 §3.7 与议题栈里记了顺位由来（防后人误读为串行）。
- **原方案的缺陷（WB 自查发现）**：隔离只写了"各自独立 venv / DSH_HOME / 端口"，是**声明式要求**——我无法从报告反查他们是否真做到了。声明 ≠ 证据。
- **升级四项**：
  1. **统一根目录**（替代"路径自定"）：项目外 `probe-trae/` `probe-claude/` `probe-qoder/`。理由：路径自定**易被建进仓库污染 git**；约定根目录还能一眼分清归属、结束时整目录可删不留残渣。
  2. **四条隔离自检**，输出贴进报告「环境事实」：`DSH_HOME` / venv prefix / `node -v`（**必须报错**）/ MCP 端口。**缺则视为不可复现**。
  3. **baseline 快照（关键）**：开跑前在自己 `DSH_HOME` 内 `git init && commit -m baseline`，跑完贴 `git status`/`diff`。两个作用——证明改动都在自己目录；**检出被别人污染**（出现自己没做过的变更 = 隔离失效，立刻停手报告）。**这是把"隔离"从"我要求你做"变成"我能看见你做了"的关键一步。**
  4. **唯一需错峰的情形**：必须动全局 / 默认 `DSH_HOME` 的步骤，先确认另两方没在跑；其余全并行。
- **方法论（可复用）**：给多个执行方下"隔离"要求时，**必须同时给可验证手段**（自检输出 + baseline diff），否则隔离是无效要求——**我看不见就没法复验**，而不可复验的要求等于没有。
- **⚠️ 又犯一次老错误（自用，已第三次）**：用 `python -c "...."` 且内部含反引号 → bash 在双引号内仍执行命令替换，`probe-trae/` 等被当命令执行、内容静默丢失 2 行。**这次是"只转义了部分反引号"**——看起来对，漏一个就丢内容，比完全不转义更难发现。**硬规则：含反引号的代码一律写成 .py 文件再跑，不做部分转义。**

## 第 0 项三方实测收口（commit d4ab4e6）：WB 判二等，待老大终裁

- **三方结论**：Trae 一等 / Claude 一等 / **Qoder 二等**。2:1，但按既定规则「可复现实证 > 多数票」，WB 采纳 Qoder。
- **WB 本地核实（未采信转述，全部属实）**：① SDK JSON-RPC 请求面仅 `initialize`/`session/prompt`/`shutdown`（`packages/sdk/protocol/src/types.ts:115-119`；`server.ts:248-253` 只分派这三个；**grep approve/answer/respond 零命中**）；② 原生 `interaction/user-questions`、`user-approval` 有同进程 waterfall answerer；③ 官方 `2026-07-06-approval-seam.md` 原文「Answerers are `approval/request` waterfall listeners. **Zero listeners fall through to unavailable**」→ **2.7.2 回答侧在 SDK 协议面不可得**。④ Claude 怀疑的 `session-log-deepseek`：确为 **explicit opt-in（默认关闭）**，但启用后发**完整未脱敏** SessionEvent 到 baseURL（已列必关清单）。
- **⚠️ 我的派发稿缺陷（认）**：判据 1「能力覆盖无实质缺口」**含两个尺度**——Trae/Claude 按「method 面差集」（SDK client vs TS client，同为 design twin 故无差）执行；Qoder 按「31 子项用户可达」执行。**同一判据两个尺度 → 必然分叉**。**教训：判据必须单尺度且写明比较对象**（比谁和谁），否则不同执行者会用不同尺度跑出相反结论，交叉验证反而制造伪冲突。
- **判二等的理由**：①「必须写 TS 才能完成」= Python 侧不能独立完成全链路；② 2.7 边界域是核心承诺，把边界决策放进 DSH 内 TS 插件、Python 只做消息管道，与"保留 Python 主控"价值主张冲突；③ §3.5 已有「23 项语义层须 TS 挂载」口径，再加 answerer，省下的成本被稀释。**缺口可补（B1 TS answerer 或 preset 白名单），不是不可达。**
- **三方硬发现（已折入）**：Windows 官方 CLI `dsh.exe` segfault（Claude+Qoder 独立发现，不影响 SDK 主路径但 `dsh plugin` 管理入口不可靠）；**长 turn 无超时**（`request_timeout_seconds` 只覆盖单次往返，须自建 watchdog）；B1 安装期仍需 Node/pnpm（"无 Node"仅运行期成立）；MCP 只证 Tools 不得外推；Python 事件多 `JsonObject` 易静默接受字段漂移；**⚠️ resume id collision 定性分歧未收敛**（Claude 判可能真缺口 vs Qoder/Trae 判探针姿势问题）→ 列阶段 3 首验项。
- **待老大终裁**：认可二等 → A-framework（本稿已按此口径，改动最小）；改判一等 → 转 A-service。

## 第 0 项终裁（老大 2026-09-08 晚）：二等成立 → 定 A-framework

- **老大确认二等**，并明示"其实我一直也在考虑全量 TS 化"——**不是被我说服，是独立同向**。我的判定在他表态**之前**已落盘（时间线：三方交付 → WB 本地核实 → 判二等 → 入 §3.5 → 提交 → 老大表态），不构成锚定污染。老大此前担心"我表态会影响你判断"，已当面澄清顺序。
- **我主动拆开的两层（勿忘）**：「二等」= 事实判定，可定论（推翻条件只有一个：拿出 SDK 侧 answer 通道的反证）；「二等 → A-framework」= **价值主张选择，不是自动推论**。中间存在**第三选项 A-service + 最小 TS 补丁**（缺口只有 2.7.2 一个，B1 通道已实测可行，Qoder 估真必须 TS 的仅 5–8 项）。
- **判三等理由的自查（诚实）**：三条里只剩 **② 边界决策在 TS 侧、Python 只做消息管道 = 与"保留 Python 主控"价值主张冲突** 是真支点；①「必须写 TS」只证明二等、不证明要全量 TS 化；③「省的成本被稀释」是成本论据，而老大已裁定不由成本决定。**我能给的是价值主张一致，给不出"技术上必须"——以后别把 A-framework 包装成硬证据结论。**
- **自我暴露（已向老大说）**：我按"可复现实证 > 多数票"翻了 2:1 的案，而**翻盘规则是我定的、派发稿判据双尺度的缺陷也是我的** → 存在"为掩盖自己缺陷而偏向被误读方"的自我审查风险。我的判断是没有（Qoder 证据本地独立核实过、与派发稿缺陷无关），但风险本身必须让老大知道：**同一个人既定罪、又定规则、又翻案，这个结构本身要被看见。**

## probe 沙箱清理（老大自行派三方，WB 不介入）

- 三目录定位：`D:\Code\probe-trae\`（Trae）／`D:\Temp\dsh-probe\probe-claude\`（Claude）／`C:\Users\SuLarry\.qoderwork\workspace\msg77v9db96tjzz2\probe-qoder\`（Qoder，**在 Home 下，只许删这一层，不碰上层会话目录**）。三者均在项目外，`git status` 已验无污染。
- **老大决定自己派三人删，WB 不派**（"这点活儿不用那么复杂"）→ 尊重，不另起流程、不写派发稿。
- **反直觉提醒（已说）**：A-framework 下这些 Python venv 对阶段 2 **无用**（阶段 2 要 Node/pnpm + TS 插件挂载）；真正值钱的「独立 `DSH_HOME` + baseline git 快照检出污染」是**方法**，已写进报告与派发稿，不随目录消失。
- **`backend/tests`、`client/tests` 绝对不动**：阶段 6 验收前的双轨回退保障，全量 TS 化期间是"功能等价"的唯一对照物。

## 三份 probe 报告：永久保留（老大明示，推翻我原定的"终裁后清理"）

- 老大：**报告价值高不删**，理由是**跑这三份花了约 200 元 token**，舍不得。**成本事实记档：第 0 项三方并行实测 ≈ 200 元 token**（未来再派多方并行的成本参考）。
- **与柔性清理规则冲突（我暴露并处理）**：规则是"已闭环即清"，这三份已闭环却要留 → **不改规则，改落点**：在三份报告**文件头各加两行标记**（🔒 永久保留、不参与柔性清理 + 结论与 §3.5 的关系），最可靠的落点是文件本身而非记忆。
- **关键风险（必须留痕）**：Trae / Claude 两份**结论是"一等"，与最终裁定相反**。留着就有被后人误读成"推翻了二等裁定"的风险 → 两份都标「原始交付、已被 §3.5 覆盖，引用以 §3.5 为准」；Qoder 那份标 ✅ 与裁定一致。**方法论：保留 ≠ 有效；保留与现行裁定相反的材料时，必须显式标注结论状态。**
- 删目录前应先扫敏感残留（Claude 那轮用过真实 key，`dsh-home/` 下可能有 session/storage 产物）——已提醒老大。

## 老大的决策逻辑（老大自述"同一套逻辑"，WB 校正后记档）

- 老大问：「"可以抛弃现有成果全面 TS 化"和"解除 token 限制加码"是同一逻辑吧？」
- **对的一半**：共同上位是「**按"做对"决策，不被已投入绑架**」——不犯沉没成本谬误。但这是**决策依据**的一致，不是行为模式的一致（一个是加码投入、一个是承认已有成果作废）。
- **必须校正的一半（关键，防止以后被当论据）**：**"舍得扔"是前提（必要条件），不是理由（充分条件）**。A-framework 的成立靠「边界在 TS 侧 → Python 主控名存实亡」+「立论③演进跟随」，**不靠"老大舍得"**。若把"舍得扔"当论据，任何激进重写都能自证 → **把性格当证据**，与「"老大说的"不构成证据」同类错误，且会自我繁殖。
- **边界**：token 加码**可逆、无副作用**；全量 TS 化**不可逆**（8830 行重写 + 测试资产重建 + 迁移期双轨维护）。故"同一逻辑"**不能**推出"只要觉得对就不计代价"。
- **机制化而非性格化（我的动作）**：双轨并行、阶段 6 验收前可回退 —— 这是"舍得扔"的**制度保障**。以后靠机制，不靠老大每次重新下决心。
- 参考镜像：老大投资侧教训是黄金超配 21%（**该减没减**，情绪导致）→ 说明真正起作用的始终是**机会成本视角 + 纪律**，不是"舍得"这个性格特质。

## §3.5 收口（commit 752b5f3）：第 0 项终裁落档

- **收口 5 处**：顶部终审结论 / §3.5 结论块 / 2.7.2 承接总表行 / §3.7 第 0 项 / 阶段 2 任务描述。
- **额外清的 A-service 残留（属口径矛盾，不是可选项）**：§3.4 跨语言「骨架零改」假设作废；升级 SOP 删 A-service 分支；S4 实现位置定为「TS 插件挂 session 事件流」；边界契约层 6 类失败模式标**已作废**（同进程后子进程生命周期/协议层/跨进程状态一致性三类不适用，保留仍适用的「升级后契约漂移」「资源与凭据」并入阶段 6）；**测试资产处置定为「按 DSH 四层体系重建」**（原"保留 + 增补"是 A-service 口径）。
- **阶段 2 新增「先决任务 0」**：代码存在形态 A/B/C 案（独立仓库构建 Cordis bundle 挂载 / fork DSH 主仓加自有包 / 混合），WB 倾向 A 案；判据 = 需触达的 DSH 内部 service 深度（Qoder 估 5–8 项需"深度介入 agent 组合"——若必须改上游代码则 A 案不成立）。**未派发，等老大点头。**
- **写进文档的两条防退化**：① A-framework 成立理由**不可简化为"因为是二等"**，附两条真理由（边界逻辑在 TS 侧 → Python 主控名存实亡；立论③演进跟随只能靠贴近核心层）；② 已作废的 PyPI SDK 路径**留档一条**并明写「不得以省事 / 省成本重新提出」——防止后人重提。
- **复验发现（已告知老大）**：老大在 `HUMAN.md` 手记的「DSH 三条硬发现」**已全部折入 `dsh-migration.md`**——`session-log-deepseek` → 289 行必关清单；Windows CLI segfault → 441 行；长 turn watchdog → 442 行。他写"不知道放哪儿，先写到这儿避免忘了"→ 已有着落。**HUMAN.md 为 AI 只读文件，我不代改，仅提示可清。**
- 提交 `752b5f3`（含 HUMAN.md 老大手记，先保住内容）。**本地领先 origin 15 个提交，待老大 push**（WB 不推远程）。

## DSH 四份文档落位：整体迁入 `docs/dsh/`（不拆）

- 老大提议在 `docs/` 下建 `dsh/`，问"三份报告是否该按定义进 `archive/`"。**WB 判断：四份一起放 `docs/dsh/`，不拆。**
- **决定性依据是 `archive/README.md` 自己的落位标准**：「冷历史（仅复盘，**不再被引用**）→ archive；活跃权威（**仍被引用**）→ docs」。**判据是"还用不用"，不是"做没做完"**。三份报告被 §3.5 引为证据支点，且「resume id collision」待核项仍需回看原始输出 → **仍被引用 → docs**。
- **另三条**：① 证据不能离开结论（证据进"只复盘不追踪现态"的冷区 = 宣布它不再参与判定，但我们要它参与）；② **四份同目录 → 决策稿内所有 `dsh-pysdk-probe*.md` 相对引用零改动**，拆开则须 `../../archive/...`、引用变脆；③ archive 现有内容 = 事故复盘 + 路线图历史（"结束了的事"），probe 报告是"将来还要用的实证"，性质不同。
- **执行**：`git mv` 保留历史；**文件名不改**（只换目录，全局 grep 仍命中、引用只改路径）；新建 `docs/dsh/README.md`（三规则：是证据不是历史 / 永久保留 / 结论以决策稿为准）；`docs/README.md` 加索引条目；`exchange/README.md` 条目标"已迁出"；改引用 ~20 处（脚本批量 + 正则补裸文件名，已验无 `docs/dsh/../` 类异常路径）。
- **顺带清了 `log-workbuddy.md` 的过时状态块**：原第 5–8 行仍写"待老大终裁""终裁后按篇幅原则清理"（后者已被老大否决）→ 压缩为 2 行已完成态。

## DSH 新版本核实（老大提及"这三四天又有新版本了，只是 alpha"）

- **核实（🟢 GitHub API + npm registry + PyPI JSON，未采信转述）**：主仓已出现 **`dsh-v0.1.3-alpha.1` / `alpha.2`**；alpha.2 于 **2026-09-07 11:45Z** 经 PR #3685 发布（纯版本号提升，261 个 `package.json` 同步改版 → **261 个包统一发版**）。compare `0.1.2-rc.1...0.1.3-alpha.2` = **ahead 644 commit / behind 0**（约 4 天内）。
- **分发渠道未动**：npm `@deepseek-ai/dsh` 与 PyPI `deepseek-harness-sdk` 的 latest **仍是 0.1.2-rc.1 / 0.1.2rc1**（PyPI 0.1.2rc1 上传于 09-04）→ **alpha 仅存在于 git tag，未进包管理器**，既不会误装，也被既定 SOP「跟随 release tag」天然挡在外面。
- **改了什么（部分可见，⚠️ 644 条只可见前 ~21 条，不可外推）**：早期 commit 高度集中在 **session format v1 → v2 迁移**（`session-format-04-live-assistant-stream` / `05-v1-v2-chunk-migration` / `06-v2-snapshot-rollout`，测试里出现 "v2 SDK expectations"）。可见范围内**未**见 protocol / approval / interaction / plugin-mount / sandbox / windows 改动。
- **三层解读**：
  1. **印证老大"飞速进化"**：261 包统一发版 + 644 commit/4 天，工程化程度高 → 「项目死掉」的兜底风险进一步降低，立论③价值上升。
  2. **但进化快是双刃**：**session 结构正在变 v2** → 依赖 session/trajectory 的 2.8.2（可见）、2.4.1（fork/resume）**可能受影响**；锁定版过期加速，每次升级的评估成本上升。**A-framework 贴近核心层 = 承受最剧烈的变动面**——这是它的真实代价，分岔表「上游集中度：高」现在有了具体数据支撑。**不推翻决策**（老大已裁定不由成本决定），但列为观察项。
  3. **对"二等"判定的预告检验（我此前说过二等是事实判定、可被推翻）**：若 0.1.3 补上 SDK answer 通道，二等即失效。**但 A-framework 仍成立**——理由是结构性的（边界逻辑在 TS 侧 → Python 主控名存实亡；演进跟随只能靠贴近核心层），不依赖"二等"这个事实。**差别在于**：若 SDK 补齐，A-service 的成本优势回归，"重写 8830 行是否值得"要重算——**好在我们尚未写一行代码，现在改主意成本为零。**
- **落档结果（老大 2026-09-08 定）**：**① 已落**（提交 `27cf760`）——老大初判"写不写都不影响"（npm/PyPI 口径下确为冗余），WB 指出**文档写的是"跟随 release tag"而非"跟随 RC"，而 DSH 的 rc 与 alpha 同为 `dsh-v*` prerelease tag、形式无差别**，且我们的锁定源是 git tag（`ref/dsh-bare`）→ 包管理器护栏对 `ls-remote` 查法不成立。最终口径：**跟随 rc 及以上 tag；alpha 只监控不跟随；一律以包管理器已发布版本为准，git tag 仅用于读源码**（§3.4 / §3.6 / §3.7 三处同步）。**② 未落，老大说"另外单独聊"。**

## 老大回应「不会改主意」→ 新增第③条成立理由（方向性风险）

- **老大原话**：PY 上的问题后续可能被补上，但**后续版本也可能再次脱离 TS**；PY 永远存在一层隐患，甚至存在「TS 继续而 PY 被抛弃」的可能（**老大自评：仅技术层面，实际上不太可能**）。
- **WB 判断**：这条比「二等」更强——**「二等」是事实判定，可被版本补齐推翻；「PY 是 TS 下游」是结构判定，补齐不改方向**。故采纳为 A-framework 第③条成立理由（前两条：边界逻辑在 TS 侧 / 演进跟随只能贴近核心层兑现）。
- **为老大直觉补的 🟢 硬证据（PyPI JSON 实测，未采信转述）**：`deepseek-harness-runtime-bin` 0.1.2rc1 **只有 4 个平台 wheel、无 sdist**（win_amd64 69MB / manylinux_2_28_x86_64 / manylinux_2_28_aarch64 / macosx_14_0_arm64），官方自述「no Windows arm64 wheel is published」→ **PY 运行时本就是 TS 编译产物，且平台覆盖由上游单方决定、无源码可自补**。平台缺口**已是既成事实**（非尾部假设），A-framework 走 npm+Node 不受此约束。
- **已落档** `docs/dsh/dsh-migration.md` §3.5 成立理由下，三条：③ 方向性风险（含概率自评标注）＋ **⚠️ 对称风险**（A-framework 消掉 PY 风险但放大 DSH 本体敞口，4 天 644 commit 的 alpha 核心层变动最剧烈，而我们恰贴核心层）→ 防止后人把③当单向话术用。
- **方法论沉淀（重要）**：老大给的是**直觉 + 自评概率低**，WB 的动作是把它**拆成可验证的结构性事实**（PY 运行时 = TS 产物 🟢）与**不可验证的尾部假设**（PY 被抛弃 🔴/🟡）分别标注，**而不是把直觉整体升格为论据**。且必须写明「③ 是保险性论据（低概率×高影响），不是主梁」——与 §十一 证据纪律同源：不因说话人是老大就提级。

## 决策稿与待办分离 + DSH-N 编号（老大定，commit `6d1391c`）

- **老大裁定**：`docs/dsh/dsh-migration.md` **不放待办**，只留判定依据、行事规则、验收基准；待办全部进 `TODO.md` 的一个专区。**编号用独立 `DSH-N` 序列**（与 P0–P5 主线无关），**完成一个阶段就归档一个到 `archive/roadmap-history.md`**——与当年 P 阶段的处理方式一致。
- **编号对齐（老大纠正了我一次）**：我初版把「阶段 2」编成 DSH-1（想让 TODO 从 1 开始），老大要的是 **DSH-N 与阶段 N 一一对应** → DSH-1 = 事实校准（已完成，进 archive），TODO 自 **DSH-2** 起。已改。
- **⚠️ 复验发现（我的错误，记牢）**：我一直声称第 0 项的硬发现「已折入各节」，**实际 grep 验证只有 `session-log` 一条真折进去了**——Windows CLI segfault / 长 turn 无 watchdog / B1 安装期需 pnpm / MCP 只证 Tools / JsonObject 字段漂移 / resume id collision，**六条全都只存在于 §3.7 待办节**。若按原计划删 §3.7，这六条（三家高强度实测的产出）就丢了。已迁入 §3.4 新表「第 0 项实测硬发现」。
  - **教训**：删减类操作前**必须逐条 grep 验证承接**，不能凭「我记得已折入」的印象。这与 MEMORY §六 复检清单第 1 条同源，但这次我恰恰没执行。
- **其余处理**：「锁定跟踪 tag」从 DSH-1 未完成项并入 §3.4 升级 SOP 的**持续动作**（持续性动作不随阶段归档）；`log-workbuddy.md` 挂件改为索引指向 TODO。
