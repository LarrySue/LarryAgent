# 2026-08-21 工作日志

## 三轮 UI 调整复验（老大直接对接 Trae，未走 WB 派发）
- 改动：欢迎页 logo 占位 / 新建会话按钮(+ 回到欢迎页 `selectConversation(null)`) / 侧栏标题栏精简。含一处功能逻辑：新建会话链路（`startNewChat` → `selectConversation(null)` → ChatView watch 清空 messages → 欢迎页）。
- WB 复验（读代码+亲跑）：前端全量单测 38/38 绿（含新增 appLayout.test.ts 7 项）；vue-tsc 0 类型错误；57 模块编译过；ChatView watch(175-184) 闭环确认；client/src 敏感信息零命中。
- ⚠️ `npm run build` 在本会话 Bash 被安全删除钩子(`genie-safe-delete.cjs`)拦截（卡在清空 dist 目录），与改动无关，老大本机终端跑可出真实产物。
- 正面：上轮立的"交付连带交流区一并提交"规矩本次生效，orphan M 未出现（Trae `60a98d6` 已含 log-trae.md）。

## 柔性清理
- 暗箱删 `log-trae.md` 中 web_search 派发稿（已完成+复验过，属历史派发任务），提交 `7837b22`。
- 暗箱删 `log-trae.md` 三轮 UI 调整详细报告（已完成+复验过），仅留当前状态摘要，提交 `a108635`。
- 注：a108635 曾把第一轮报告的「图片资源目录约定」外移至 MEMORY.md，但随后撤销（提交 `0f14066`）——MEMORY 是 WB 专属记忆，不该存项目通用工程事实；此类事实以代码注释/Grep 为准。老大点出根因：UI 约定分散在 Marvis 交流文件 / log_design / Trae 上下文，病态，需单一权威参考。

## 建立 UI-Reference 权威文档（治愈 UI 约定分散病态）
- 新建 `exchange/public/UI-Reference.md` 为 UI 设计单一权威参考（提交 `22808d5`）。v1 由 WB 从 `log_design.md`(设计精化定案)+`log-marvis.md`(产品方向)+`tokens.css`(代码数值) 汇聚，去过程留定案。
- 分层同治理线：UI Reference(权威成品) ↔ 交流区(过程)，与约束↔交流、HUMAN↔HUMAN_NOTE 同源——根因都是「知识/接口没单一定位→AI 靠推断→混乱」。
- 同步：tokens.css 顶部注释权威源改指向 UI-Reference；WORKBUDDY 文件地图加索引。Trae 未固化实现决策以代码为准，待点将回写；未动 log_design/log-marvis 专属文件。
