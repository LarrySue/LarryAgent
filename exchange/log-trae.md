# Trae 协作区

## 【派发 · 2026-09-08】阶段 2 第 0 项：Python SDK 实测（一等 / 二等公民判定）

**状态：🔄 进行中。当前最高优先，优先于一切其他事项。**

### 背景（三句）

- `exchange/dsh-migration.md` **已定稿**（老大 09-08 终审通过）：A 路径（换底座）拍板，§1.5 / §3.0 / §3.7 均已确认无问题。
- A 的最终形态分两岔：**A-framework**（全量 TS 化）vs **A-service**（Python 后端 + DSH 子进程）。**选哪岔只由「Python SDK 是不是一等公民」决定**——老大裁定：若属二等公民，省成本也不取，直接全面贴近核心层（含语言）。
- 你已完成预检一半（🟢 锁定版本地核实，第 0 项性质由「未知」变为「官方一等路径的测绘」）。本任务是**跑完剩下的一半并给出判定**。

### 你要回答的唯一问题

> **Python SDK 路径，相对原生 TS 路径，是不是一等公民？**
>
> 输出只能是三选一：**一等 / 二等 / 无法判定**。「大概」「倾向」「应该」不接受。判「无法判定」时须写明**缺哪条证据才能判定**。

### 判定操作化（WB 细化；若你认为阈值不合理，说明理由并给替代口径——不要闷头执行你觉得错的判据）

**一等 = 下列四条全满足**：

1. **能力覆盖**：A 步拿到的 JSON-RPC 方法面，相对 TS 侧暴露面**无实质缺口**。判据：31 子项中任一必需能力在 Python 侧拿不到，即为实质缺口；差集为空或只含我们不需要的能力 → 通过
2. **挂载通道**：B 三通道中 **B3 MCP 桥可行**（决定 shell / file_ops 能否留在 Python），且 B1 / B2 至少一条可行
3. **官方支持度**：官方文档 / CI smoke / 发布同 train 三项中至少两项有一等表述或证据（预检已占两项）。**实测与文档冲突时以实测为准**
4. **可独立运行**：**在无系统 Node.js 的环境下**完整跑通（验证方法见下）

**二等 = 任一条不满足。无法判定 = 证据不足以覆盖上述四条。**

### 三步执行

完整规格见 `exchange/dsh-migration.md` §3.7「第 0 项」（**以该处为准**；此处只列执行要点与新增约束）：

- **A 连通**（~半天）：`pip install deepseek-harness-sdk deepseek-harness-runtime-bin==0.1.2rc1`；`sdk` 与 `sdk-minimal` 两 profile 各跑一遍官方最小示例；**产出 JSON-RPC 方法面清单**（判定的原始数据，务必完整记录）
- **B mount 三通道测绘**（1–2 天）：B1 TS bundle（`dsh plugin --profile sdk add`）／B2 `patch.yml` 调用级叠加／B3 **MCP 桥**（Python 侧起最小 MCP server 供 DSH mcp-client 消费）。同时测**事件流粒度**：`on_notification` 是否含 tool 调用详情（= 2.4.2 记忆双写的数据源判据）
- **C 判定**（~1 天）：按上述四条逐项给结论 → 出最终判定 → 给落地形态建议

### ⚠️ 两条执行硬约束（做不到就判「无法验证」，不要含糊过去）

1. **「无需系统 Node.js」必须真验**：验证时把 Node 从 PATH 移除（或临时改目录名），跑通后还原。**机器上装着 Node 就等于没验证这条**——这条声明是我们不必管 Node 依赖树的全部依据。
2. **禁用真实 API key**：跑 demo 需要的模型凭据**一律走环境变量**；**禁止**使用 `config.yaml` 里的真实 key，**禁止**出现在任何报告 / 日志 / 命令行回显 / git 提交中。用临时测试 key 或 mock；官方示例若支持 offline / mock 模式优先用它。

### 交付

新建 **`exchange/dsh-pysdk-probe.md`**（独立文件，不写在本协作区；WB 折入结论区后清理），完成后在本文件回写一句「已交付，见 `dsh-pysdk-probe.md`」。

报告须含七块，缺一不可：

1. **环境事实**：OS / Python 版本 / 系统 Node 情况及屏蔽方式 / 所装包与版本 / 实测日期 / DSH 锁定版 `0.1.2rc1`
2. **A 结论** + **JSON-RPC 方法面清单**（原始数据）
3. **B 三通道结论表**：B1 / B2 / B3 各自 可行 / 不可行 / 部分可行 + 证据
4. **事件流粒度结论**：`on_notification` 是否含 tool 调用详情
5. **C 判定**：四条逐项结论 + **最终判定（一等 / 二等 / 无法判定）**
6. **落地形态建议**：A-framework 还是 A-service，一句话理由
7. **意外发现 / 阻塞 / 你认为我判断错了的地方**

**所有事实断言须标证据等级** 🟢实测 / 🟡官方源 / 🔴推断。🔴 不得作为判定依据，只能列为待补证据。**不要复述 README 原文当证据而不自己跑一遍**——上一轮你报的四条发现，我核实后三条属实、一条（win-x64 model-visible 快照）在锁定版不存在。

### 不做的事（避免跑偏）

- **不写实现代码**——这是测绘与判定，不是 prototype。探针 / 示例脚本可以写，产物不进产品代码
- **不引入任何第三方插件**——按 §3.0 只借鉴不直装。本任务除官方 SDK 与 runtime-bin 外，**不要 `dsh plugin add` 任何社区插件**
- **不改 `dsh-migration.md` 结论区**（§1/§2/§3）——结论区已定稿。认为有错就在报告第 7 块指出，由 WB 折入
- **不 checkout DSH 源码**——本地已有只读裸仓库 `ref/dsh-bare`（锁定 `dsh-v0.1.2-rc.1`），查证走它；Windows 下整体 checkout 会卡死（实测 5.5 小时未完成）。查阅规则见 `dsh-migration.md` §2.2
- **装包装在项目外的独立 venv**，别污染仓库；提交前 `git status` 自查，venv / 缓存不得进 git

### 完成标准

老大拿到报告，能直接依据第 5 块拍板 A 的最终形态——**不需要再问你「所以到底是不是一等公民」**。
