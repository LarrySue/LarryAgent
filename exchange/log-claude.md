# Claude 协作区

## 【派发 · 2026-09-08】阶段 2 第 0 项：Python SDK 实测（一等 / 二等公民判定）— 独立执行

**状态：🔄 进行中。老大 2026-09-08 点名派发，第二顺位。当前最高优先，优先于一切其他事项。**

> **指令来源**：本任务由老大**点名指派（口头）**；以下规格为 WB 细化。**若与你收到的老大口头指令冲突，以老大口头为准，并立即在报告中指出冲突点**——不要自行折中消化。

老大裁定此事重大，**点名三方并行独立执行**（Trae 第一 / 你第二 / Qoder 第三）。你与 Trae 跑的是同一件事，**但视角不同**：Trae 走全量主路径，你按你的测试本职走**边界与失败面**。三方结论由 WB 交叉比对。

### 背景（三句）

- `exchange/dsh-migration.md` **已定稿**（老大 09-08 终审）：A 路径（换底座）拍板。
- A 的最终形态分两岔：**A-framework**（全量 TS 化）vs **A-service**（Python 后端 + DSH 子进程）。**选哪岔只由「Python SDK 是不是一等公民」决定**——老大裁定：若属二等公民，省成本也不取，直接全面贴近核心层（含语言）。
- 你的角色是**代码测试 AI**（验证与测试为本职），本任务是**能力测绘 + 失败面验证**，不越界。你上一轮提的**边界契约层 6 类失败模式**正是本次要实测的对象。

### 你要回答的唯一问题

> **Python SDK 路径，相对原生 TS 路径，是不是一等公民？**
>
> 输出只能是三选一：**一等 / 二等 / 无法判定**。「大概」「倾向」「应该」不接受。判「无法判定」时须写明**缺哪条证据才能判定**。

### 判定操作化（WB 细化；依你的行为准则，认为规格有错就**停下来暴露并给替代口径**，不要闷头执行你觉得错的判据）

**一等 = 下列四条全满足**：

1. **能力覆盖**：拿到的 JSON-RPC 方法面，相对 TS 侧暴露面**无实质缺口**。判据：31 子项中任一必需能力在 Python 侧拿不到，即为实质缺口
2. **挂载通道**：B 三通道中 **B3 MCP 桥可行**（决定 shell / file_ops 能否留在 Python），且 B1 / B2 至少一条可行
3. **官方支持度**：官方文档 / CI smoke / 发布同 train 三项中至少两项有一等表述或证据。**实测与文档冲突时以实测为准**
4. **可独立运行**：**在无系统 Node.js 的环境下**完整跑通

**二等 = 任一条不满足。无法判定 = 证据不足以覆盖上述四条。**

### 执行范围

- **A 连通**（~半天）：`pip install deepseek-harness-sdk deepseek-harness-runtime-bin==0.1.2rc1`；`sdk` 与 `sdk-minimal` 两 profile 各跑一遍官方最小示例；**产出 JSON-RPC 方法面清单**（判定原始数据）。完整规格见 `exchange/dsh-migration.md` §3.7「第 0 项」，以该处为准。
- **B mount 三通道测绘**（1–2 天）：B1 TS bundle（`dsh plugin --profile sdk add`）／B2 `patch.yml` 调用级叠加／B3 **MCP 桥**（Python 侧起最小 MCP server 供 DSH mcp-client 消费）。同时测**事件流粒度**：`on_notification` 是否含 tool 调用详情（= 2.4.2 记忆双写的数据源判据）。
- **C 判定**（~1 天）：四条逐项结论 → 最终判定 → 落地形态建议。

### 你的专属侧重（这是你比另外两方更有价值的地方）

**① 边界契约层 6 类失败模式——逐类给「能否实测 + 结果」**（你上一轮提的，本次验证它是否真如预判）：

| 类 | 验证什么 |
|---|---|
| 子进程生命周期 | 启动失败 / 崩溃 / 僵尸进程 / 重启后能否 resume |
| 协议层 | 版本不匹配、方法不存在、错误码 → `LarryException` 映射是否可行 |
| 超时与取消 | abort 能否透传、有无资源泄漏 |
| 状态一致性 | 双写崩溃时**哪边先写**、重试是否幂等、会话 ID 双向映射 |
| 升级后契约漂移 | RPC 方法面快照能否 diff（这是升级回归的输入） |
| 资源与凭据 | 句柄泄漏、**key 是否出现在 RPC 日志或错误消息里** |

**② 不只证明"能跑通"——主动找"什么情况下会挂"**。跑通是及格线，找出失败面才是本次的价值。

**③ 可复现性（你的硬要求）**：报告必须含**可复跑步骤 + 原始输出**。三方结论不一致时以可复现实证为准、不以多数票为准；给不出复现步骤的一方，结论降为 🔴。

### ⚠️ 两条执行硬约束（做不到就判「无法验证」，不要含糊过去）

1. **「无需系统 Node.js」必须真验**：验证时把 Node 从 PATH 移除（或临时改目录名），跑通后还原。**机器上装着 Node 就等于没验证这条**——这条声明是我们不必管 Node 依赖树的全部依据。
2. **禁用真实 API key**：跑 demo 需要的模型凭据**一律走环境变量**；**禁止**使用 `config.yaml` 里的真实 key，**禁止**出现在任何报告 / 日志 / 命令行回显 / git 提交中。用临时测试 key 或 mock；官方示例若支持 offline / mock 模式优先用它。另：**验证 key 是否会泄漏进 RPC 日志或错误消息**（属上述第 6 类失败模式），这是凭据威胁模型的直接输入。

### ⚠️ 三方并行：环境隔离 + 独立执行（必读）

1. **各自独立 venv**（项目外，路径自定），不共用、不互相装包。
2. **各自独立 `DSH_HOME`（必做）**：`dsh plugin --profile sdk add` 会把持久 patch 写进 `$DSH_HOME/profiles/sdk/cordis.patch.yml`——**三人共用同一 `DSH_HOME` 会互相覆盖 patch，产出互相污染的结果**。用环境变量把 `DSH_HOME` 指到自己的目录，并在报告「环境事实」里写明路径。
3. **MCP 探针用不同端口**，避免端口占用导致假失败。
4. **先独立跑完、落盘报告，再交叉比对**——**不要先看别人的报告再跑**（那样就成了锚定，交叉验证失效）。交叉比对由 WB 做。
5. 只写自己的报告文件，不改他人报告、不改 `ref/dsh-bare`（只读）；提交前 `git status` 自查。

### 交付

新建 **`exchange/dsh-pysdk-probe-claude.md`**（Trae 与 Qoder 各写各的，互不覆盖）。完成后在本文件回写一句「已交付，见 `dsh-pysdk-probe-claude.md`」。

报告须含七块，缺一不可：

1. **环境事实**：OS / Python 版本 / 系统 Node 情况及屏蔽方式 / 所装包与版本 / **`DSH_HOME` 路径** / 实测日期 / DSH 锁定版 `0.1.2rc1`
2. **A 结论** + **JSON-RPC 方法面清单**（原始数据）
3. **B 三通道结论表**：B1 / B2 / B3 各自 可行 / 不可行 / 部分可行 + 证据
4. **事件流粒度结论**：`on_notification` 是否含 tool 调用详情
5. **C 判定**：四条逐项结论 + **最终判定（一等 / 二等 / 无法判定）**
6. **落地形态建议**：A-framework 还是 A-service，一句话理由
7. **边界契约层 6 类失败模式表** + 意外发现 / 阻塞 / **你认为我判断错了的地方**

**所有事实断言须标证据等级** 🟢实测 / 🟡官方源 / 🔴推断。🔴 不得作为判定依据，只能列为待补证据。

### 不做的事（避免跑偏）

- **不写实现代码**——这是测绘与判定，不是 prototype。探针 / 测试脚本可以写，产物不进产品代码
- **不引入任何第三方插件**——按 §3.0 只借鉴不直装。除官方 SDK 与 runtime-bin 外，**不要 `dsh plugin add` 任何社区插件**
- **不改 `dsh-migration.md` 结论区**（§1/§2/§3，已定稿）。认为有错就在报告第 7 块指出，由 WB 折入
- **不 checkout DSH 源码**——本地已有只读裸仓库 `ref/dsh-bare`（锁定 `dsh-v0.1.2-rc.1`），查证走它；Windows 下整体 checkout 会卡死（实测 5.5 小时未完成）。查阅规则见 `dsh-migration.md` §2.2
- 依你的行为准则：完成一个可验证单元即 `git commit`，保持工作区干净

### 完成标准

老大拿到报告，能直接依据第 5 块拍板 A 的最终形态——**不需要再问你「所以到底是不是一等公民」**。
