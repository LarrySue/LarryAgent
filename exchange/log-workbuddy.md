# WorkBuddy 协作区

## 当前议题栈（老大 2026-09-06 裁定：栈型推进，上一层不定案不进下一层）

1. **✅ 产品定位（已定稿落位）** — 8 域 / 31 子项能力树，2026-09-06 定稿并迁出活区，权威版见 **`docs/product-positioning.md`**（README 存指针）；编号体系为全局十进制多级（`1` / `2.3` / `2.3.1`）。四方讨论记录留在该稿「附：决策追溯记录」。
2. **✅ DSH 迁移（A 路径已拍板 + 两段式重构）** — `dsh-migration.md` 按 `product-positioning.md` 同款两段式重组：§1/§2/§3 结论区（WB 维护）+ 讨论区（历史过程章节已按只留结论原则删除）；老大同时裁掉 §零 项目简介（评估须 Trae / Claude）与时间节点描述（项目时间不重要）。结论区路径决策与阶段 1–6 实施规划保持不变；**新增 §3.0 硬约束「第三方引入原则」（老大拍板）：社区 / 第三方插件只可借鉴（读设计 / fork 自改 / 隔离环境临时验证），不可直接纳入为运行时依赖，企业级维护者亦然；边界——DSH 底座是框架依赖不是插件，A 路径不受影响**；**新增 §1.5「押注主体依据（老大明示 · 信念层）」**：最根本理由是对深度求索公司的信任（"比较唯心但有依据"），定性为**信念层不是证据**（去掉它 A 仍成立），与 MIT + 可 fork 兜底并列——**信任决定"选谁"、兜底决定"选错的代价"**，并列明核心矛盾（AGI 为重心 ⇒ harness 是副产品，故兜底不能因信任而放松）与四条可证伪失效条件；**Marvis 第 2 轮完整版已补收口（§5.6）**，其中「31 子项产品承诺无降级」补入第 0 项验收标准（WB 原收口遗漏）；**仍待老大拍板（Python SDK 实测 / 升级 SOP 节拍 / prototype 派发 / 差异化能力优先级 / fork 代码进库规范 / 记忆 schema 是否设为阶段 4 派发门禁——WB 倾向维持"优化项非门禁"）**
3. **⏸ 其余讨论稿（各自独立排期，未进栈）** — `web-search-design.md`（搜索选型，未展开）／`deployment-architecture.md`（云部署草案）／`discussion-time-context.md`（时间上下文）。

## UI Designer 待办（Logo 资产，待用户处理）

- 导出 .ico 格式（Tauri 窗口图标需要）
- 导出多尺寸 PNG（16/32/48/64/128/256）
- AI 生成水印需去除后才能作为正式资产

## 时间上下文专题讨论（2026-09-03 建）

- 新建 `discussion-time-context.md`：综合 Marvis「对话时间对齐」提案（09-03 恢复）+ WB 四层坑分析，作跨 AI 讨论底稿（非定案）。
- 综合结论：无不可跨越的坑；便宜层（created_at + 注入）随时可做，唯一硬骨头 = Marvis 点出的「压缩拍平时间轴」，归 P5 记忆保鲜。
- 基础设施：SQLite 三表时间戳已具备；唯一硬缺口 = ChromaDB payload 补 `created_at`（写入后不可改、须重灌）；另需定 UTC 存储约定。

## 云部署架构方案（2026-09-03 产，待老大确认）

- 老大指示：结合 TODO 已有上云计划，统一出「部署方案细化」，厘清哪些上云、哪些在客户端、开发/生产两态、配置、隔离、切换、打包。
- 产出 `exchange/deployment-architecture.md`（草案，待定稿后回 `docs/`）：结论 = 云脑（LLM 编排/云端记忆/web_search/鉴权）+ 本地手（file_ops/shell/本地文件记忆/工作空间/本地库），并给出三阶段渐进路径（现态单机 → 第一版上云只出脑能力 → 本地能力下沉后全套）。
- 关键实证（对照代码）：① 前端 `API_BASE="/api"` 相对路径，生产 Tauri 会断链，须改可配置；② `shell`/`file_ops` 上云后因 `caller_ip` 公网 IP 不在白名单而天然失效，印证「本地工具须下沉」；③ `LARRY_CONFIG` 即现成的环境切换底座。
- 待议（未展开）：本地工具下沉实现路径（双向通道 vs 本地 agent 运行时，倾向后者）、记忆系统分层、多端设备身份、移动端能力不对称。
- 现状 gap（已纳入方案 §七.1 待议）：file_ops/shell 目前在 backend，上云后会操作云端机器而非本机，需下沉 client 端。

