# Marvis 交流区

---

## 当前任务（2026-08-15 WorkBuddy 派发）

### P4.35 — 界面基调定义（产品视角，出初稿）

P4 前端组件开工前需要一份界面基调约定，避免每个 Vue 组件各写各的样式、写完再返工。你出初稿，老大审定，Trae 按 规格 实现。

**产出：一页 markdown 设计约定**，写到你的交流区即可，包含：

1. **布局结构**：左会话栏 + 右消息流是成熟模式（参照主流聊天应用），定各区域比例和响应式断点（P5 移动端复用，响应式 P4 一次做对——你在 v6 里提的这条已定案）
2. **配色基调**：亮色/暗色（或双主题）、主色选择、消息气泡双方配色
3. **字体**：中文为主场景的字体栈、字号层级
4. **组件风格**：气泡、工具调用卡片、输入框、侧栏条目的视觉风格（圆角/边框/间距的统一约定）
5. **多角色差异化呈现方案**（重点）：LarryAgent 的差异化是 health/finance/default 多角色——是否用主题色/头像/问候语区分角色？给方案和理由。这是这个产品和"随便一个 ChatGPT 套壳"拉开距离的地方

**边界**：这是基调约定不是设计系统，一页纸量级，别过度展开。视觉稿用文字描述即可，不需要图片。

---

---

## P4.35 界面基调初稿（产品视角交付，2026-08-15）

### 0. 产品调性（先定调，约束后面所有选择）

LarryAgent 是个人效率工具，不是社交产品，也不是"ChatGPT 套壳"。三个词：**克制、专业、可信赖**；核心美学目标一句话——**克制的高级感**（中性灰阶、低饱和、靠层次与留白，不铺彩色）。

- 克制：界面服务于"快速进入对话"，不过度装饰、不炫技
- 专业：信息层级清晰，工具调用过程可见（Agent 产品的差异化，区别于普通聊天）
- 可信赖：暗色基调 + 稳定语义色，传递"私人助理"而非"玩具"的感觉

### 1. 布局结构

左会话栏 + 右消息流，成熟模式，不创新。

- 左栏：会话列表（含角色标识），默认 260px、可折叠
- 右主区：消息流（上）+ 输入区（下）
- 顶部：会话标题 + 角色切换入口

响应式（产品要求，非具体断点）：桌面双栏；移动端左栏收为抽屉/底部导航、右主区全宽，核心对话流程不断。断点具体数值归 UI Designer。

### 2. 配色基调

**暗色为主，亮色后置**（P4 只做暗色）。理由：个人助理类工具暗色耐看、长时间不疲劳，且 chat.html 已有暗色基因，精化而非推翻。

- 主色：偏克制的灰（中性灰阶，靠灰度深浅建立层次），**替代 chat.html 的霓虹绿**；不设大面积彩色主色
- 用户气泡：灰阶中偏亮一档（归属感）；AI 气泡：中性灰（不抢焦点）
- 语义色：成功/警告/错误保留标准三色，仅状态出现时使用、不常驻
- 彩色只作"可辨识的视觉点"（角色标识、状态），不铺大面积

具体色值交 UI Designer 精化为 token，产品只定方向。

### 3. 字体

中文为主场景，字体栈优先系统中文（Microsoft YaHei / PingFang SC），英文数字用 Inter/SF。字号：正文 14px、标题 16-20px、辅助 12px；中文行高 1.6 舒适区。

### 4. 组件风格

统一约定（基调，非设计系统）：圆角/间距/阴影/过渡统一为 design token，由 UI Designer 落成 CSS 变量。

- 气泡：用户右对齐主色、AI 左对齐中性色
- **工具调用卡片（灵魂组件）**：可折叠，展示"工具名 + 状态（执行中/成功/失败）+ 结果摘要"。让用户"看得见 AI 在干什么"——呼应原则 4（明确安全边界，让用户知道 AI 的动作）
- 输入框：多行自适应，Enter 发送 / Shift+Enter 换行
- 侧栏条目：会话标题 + 角色色点 + 时间

### 5. 多角色差异化呈现（重点）

**产品意义**：记忆系统 + 多角色是 LarryAgent 区别于"ChatGPT 套壳"的唯一壁垒。差异化不是装饰，是"让用户一眼知道现在在跟谁对话、这个角色是干什么的"的心智锚点。

方案（轻量，不做三套换肤）：

| 角色 | 标识色 | 语义 |
|---|---|---|
| default | 中性灰（无彩） | 通用、中性、可靠 |
| health | 低饱和绿 | 温和、可信赖（健康语义） |
| finance | 低饱和琥珀 | 专业、警觉、数据驱动（金融语义） |

落地方式：侧栏条目角色色点 + 顶部角色专属问候语 + AI 气泡左侧色带 + 工具卡片 header 色跟随角色 + 切换时轻微过渡（色带 slide + 问候语 fade）。

**明确不做**：三套完整主题换肤（成本高、维护噩梦，违背"成本不能上天"）。

**理由**：差异化要"可感知但不喧宾夺主"。用户核心诉求是高效对话，不是换皮肤。轻量色标识 + 问候语足以建立"角色切换"心智，且不增认知负担。

### 6. P4 首版功能优先级（产品视角补充）

- **必做**：会话列表（增/删/切换）、消息流（SSE 流式 + 工具调用卡片）、输入框、角色切换入口
- **后置**（P5 或后续）：记忆管理 UI、设置页、亮色主题、logo/图标资源

---

---

## P4.35 结构层补充决策（老大拍板，2026-08-15）

- **底部状态栏：砍掉**。UI Designer 文档中的 24px 底部状态栏不保留（个人自用，无连接状态/版本号展示需求）。
- **设置入口：仅顶栏右上角**（UI Designer 文档已有），侧栏底部不放设置入口，避免重复。
- **色彩：文字阶段不深究**。最终以渲染效果为准，老大看实际效果后再定怎么改；角色色先按 UI Designer 版本跑。

---

**分工边界**：产品初稿只定方向；design token 化、边界状态、响应式断点数值、Accessibility 由 UI Designer（workBuddy_ui.md）接手精化。

---

## 对话时间对齐需求（老大提出，2026-08-15，产品视角提案）

**问题**：长期会话隔几天再聊，AI 仍以为停在旧日期；时间是对话中"不提也在调用"的隐式上下文，同一句话在不同日期/时刻语义可能相反。老大困惑为何所有 AI 工具普遍存在此问题、怀疑有技术坑。

**本质判断**：无不可跨越的技术坑，是三因素叠加——① 只注入"当前日期"、未给每条历史消息标绝对时间，模型缺相对时间锚点；② 记忆压缩把时间维度拍平（最深坑，非模型笨，是压缩阶段丢时间轴）；③ 隔天续聊属长尾低频，大厂无动力深做。

**可落地方案（分层）**：
1. 消息持久化带 `created_at` 绝对时间戳
2. 注入上下文时标相对时间 + system prompt 注入当前时刻
3. 记忆压缩保留时间锚点，不拍平
4. 工具兜底 `get_current_time`，需精确时刻显式拉取
5. 续聊注入"距上次对话已过 X 天"

**优先级**：核心归 P5（记忆保鲜，同一件事）；建议 P4.3 会话列表/消息持久化时顺手埋 `created_at`，成本近零，避免 P5 返工。

---

## 历史记录

**Marvis 宏观视角 v6 摘要（2026-08-15）：P4 开始前的想法**

- 响应式一次做对 → 已定案（Q7），P4.2 规格已含
- Tauri vs pywebview 质疑 → 老大裁决继续 Tauri（骨架已备、真 exe、否决 pywebview/Electron）
- 进程生命周期三坑 → 已具体化进 P4.1 规格
- chat.html 复用 → 已入 P4.4 规格；
- 未预期异常出口 → 已入 P4.6
- 测试债务提醒（P5 上云前处理）→ 已记 TODO 工程债务段，不阻塞 P4

---

## 项目实施整顿验收（2026-08-17，Marvis 宏观视角）

**结论：整顿闭环，无阻塞，可继续推进 P4.4。**

**已落地（全部已提交）：**
- AI-GOVERNANCE.md 定稿 v2（四方收口，含砍/缓/留决策账本）
- 三份 Tier 1 约束文件就位：CLAUDE.md / TRAE.md / WORKBUDDY.md，顶部均含 Tier 0 4 条 + 复验铁律
- TODO.md 瘦身（P0-P3 迁 archive）+ archive/roadmap-history.md 归档
- HUMAN.md 拆分（约束/记录）+ HUMAN_NOTE.md 新建
- HUMAN.md 已进 CLAUDE/TRAE 行为准则（read-first 清单落地）

**唯一未落地：**
- conftest.py 测试隔离程序性强制（22:00 已派发 Claude，实施中未回写）——等回写即可，不阻塞

**待观察：**
- 工程债务选项 A/B 待讨论（TODO 已标注"找机会讨论"）
- P4.4 聊天界面（Trae 实现 / Claude 审查，派发中）
- 记忆系统调优、多场景 AI 架构（长期）

**产品视角一句话**：机制层收口符合"保留 Tier 0 + 测试隔离 + 按天归档，其余缓/砍"的定调；接下来重心应回到产品能力层（P4.4 界面落地），别再往机制层加码。

— Marvis
## conftest.py 验收通过（2026-08-19，Marvis 实证，机制层闭环）

- 交付物：`backend/tests/conftest.py`（commit 5cd2104），逻辑符合 AI-GOVERNANCE §5.3：收集前设 LARRY_CONFIG→临时 yaml（临时 db + chroma、关 vector_store）、autouse 断言防真实库、atexit 清理含 key 临时目录；`config.py:114` 确认读 LARRY_CONFIG 覆盖路径。
- 实证：跑两轮全套测试，真实库 `data/larry.db` 零触碰（200704B / 15:03:12 冻结），临时 `larry_test_*` 目录每次新建 → 隔离真实生效（验证行为非动作），P3 test_chat_service 存量污染口已堵。
- 附带发现（工程债，非阻塞）：本机 Python 3.11.8 + pytest 9.1.1 下，既有手写 `asyncio.get_event_loop()` 测试大面积报 "no current event loop"，与 conftest 无关，是测试基建兼容问题；Claude 全绿在其自身环境成立。建议后续统一 asyncio 测试约定。
- **机制层结论：全部闭环，放行 Trae 开工 P4.4。**
## P4.4 UI 测试边界判断（2026-08-19，Marvis 产品/用户视角）

**问题**：老大确认 UI 层 Claude 能否独立完成测试、是否需要用户参与。

**现状事实**：
- 前端测试基建为零：client/package.json 无 vitest/vue-test-utils/jsdom，无测试配置、无测试文件、node_modules 无 vitest。
- Claude 测试交付史全在后端 pytest（P4.3 17/17、P4.6 11/11），前端 Vitest 栈无先例，属新领域。

**分层判断（谁能测什么）**：
| 层 | 内容 | 谁测 |
|---|---|---|
| 逻辑层 | 组件渲染 / parseSSE / 工具卡片状态机 / 输入框快捷键 / 错误体解析 | Claude 能（搭 Vitest + mock Tauri 后） |
| 集成层 | 会话切换加载 / 角色切换传参 / 鉴权头 | Claude 能，依赖 mock 质量，易翻车 |
| 真机层 | Tauri 窗口 / SSE 流式实跑 / 后端 500 降级 / 视觉效果 | 必须老大收尾（规格已标"待 GUI 环境验证"） |

**给 Trae 的建议（低成本高收益，堵 Claude 测试坑）**：UI 组件与 Tauri 调用（window.__TAURI__）做薄解耦，封装一层 adapter。同时降低 Claude 测试 mock 难度 + 后续维护成本。

**老大的参与点**：现在无需动作；等 Trae 实现 + Claude 测试交付后，真机跑一遍验收（对话流/工具卡片/角色切换/错误态），以实际渲染效果为准（既定原则）。
