# LarryAgent 产品功能边界（树状版）

> **状态**：讨论稿（活区）。**权威版本为本文件**，README 开头不再平铺条目、只保留指向本文件的指针；本稿结构经老大确认后回 `docs/`，README 指针同步更新。
>
> **缘起**：老大 2026-09-06 亲述功能边界（初版 8 条 → README 定稿 14 条），并要求升格为独立文件、重整为能力树、逐条写实落地方式。本稿在 14 条基础上，综合 **Claude / QoderWork / Marvis / Trae** 四份「README 定位段补充意见」重整而成（各方原意见见 `exchange/log-*.md`）。
>
> **范围**：仅**产品能力层**。项目实施目标与协作机制层不在此列（老大 2026-09-06 裁定：数据安全可恢复、评估与迭代度量属项目实施层，不进功能边界）。

## 读法

**状态图例**

| 标记 | 含义 | 落地方式怎么写 |
|---|---|---|
| ✅ | 已实现 | 写**怎么做的**（代码/配置事实） |
| 🚧 | 部分实现 | 分别写已做的部分与缺的部分 |
| 📐 | 有计划（TODO 有条目 / 讨论稿有方案，但无排期） | 写**怎么计划的**（引用来源） |
| 🌱 | 愿景，**无承接**（TODO 无条目、exchange 无稿、无设计） | **不写**（如实标注无承接） |

**编号规则**：`域.子项`。顶层 8 个域，每域下为子项（能力 / 功能 / 模块）。

---

## 顶层总览

| 域 | 名称 | 子项 | 最弱一环 |
|---|---|---|---|
| 一 | 交互与会话 | 3 | 1.3 🌱 |
| 二 | 记忆与知识 | 6 | 2.6 🌱 |
| 三 | 能力扩展 | 3 | — |
| 四 | 专家与路由 | 2 | 4.2 📐 |
| 五 | 边界与约束 | 6 | 5.6 🚧 |
| 六 | 可见性与掌控 | 3 | 6.3 🚧 |
| 七 | 质量与可靠性 | 3 | 7.2 🚧 |
| 八 | 形态与部署 | 3 | 8.1 📐 |

---

## 一、交互与会话

### 1.1 会话生命周期管理 🚧

**已做**
- 会话 CRUD、重命名、归档（`GET /conversations?archived=`）
- 回收站软删除：`GET /conversations/trash` + `restore` / `purge`，删除可恢复

**计划**（`TODO.md`「UI 页面三件套」）
- 三个 Vue 页面：设置入口（复活）/ 已归档列表 / 回收站列表。后端 API 与前端 `api.ts` 函数均已就绪，**仅缺页面**，UI 复用活跃列表骨架
- 老大 2026-08-27 定调三者同期做

**无承接**：HUMAN 待办提过「批量归档 / 批量删除」，**未进 TODO**，无排期

### 1.2 对话体验 🚧

**已做**
- SSE 流式输出 + 停止生成（P3 核心交付）

**计划**（`TODO.md` UI/UX 段 + `docs/ui-reference.md` §5.4 / §6）
- 会话项时间戳展示（API 已返 `updated_at`，只差渲染）
- SSE 中断恢复：流中断 5s 无数据 → 提示「重新发送」
- 网络断开由临时 toast 改常驻 banner
- 超长消息折叠：>2000 字默认折叠前 6 行 + 展开全文（优先级低）

### 1.3 会话级作用域（沙盒） 🌱

仅存在于 `HUMAN.md` 待办（「会话指定工作空间，可能类似每个会话有一个沙盒」）。TODO 无条目、无设计稿。

> 注：此条不是 UI 列表功能，落地质变为**数据模型 + 工具作用域 + 隔离策略**。老大 2026-09-06 判断「会话管理偏 UI 体验、不值得列」，但保留了「会话为单元管理」条目；本子项为其能力侧留位。

---

## 二、记忆与知识

> 本域由 Claude 主张升格（「项目主线是越来越懂你的记忆闭环，一行装不下」），四份意见均认同记忆需带子结构。

### 2.1 短期记忆 ✅

会话内消息历史，超长按 `llm.max_input_tokens` 截断旧消息（见 7.2 — 该机制本身是一致性问题的根源）。

### 2.2 长期记忆闭环 ✅

- **提取**：`memory/archiver.py::generate_summary` — 归档时 LLM 生成摘要（`POST /api/memory/archive`）
- **人审**：`POST /api/memory/archive/confirm` → `confirm_and_store`，**确认（可编辑）后才入库**
- **双写**：SQLite 为真相源 + ChromaDB 向量库（可由 SQLite 重建）；Embedding 用本地 bge-small-zh
- **召回**：新对话按语义相似度检索（`memory/engine.py::get_long_term_memory`），拼入上下文
- **降级**：ChromaDB 不可用时静默降级，不阻塞对话

### 2.3 记忆可管理 🚧

**已做**：`GET /api/memory` 列表可查；`DELETE /api/memory/{id}` 删除；`is_active` 停用。编辑发生在 confirm 入库前。

**承诺档位澄清**（QoderWork 指出「（可管理）」括号超额承诺）：当前档位 = **可看 / 可停用 / 可删 / 入库前可编辑**，**不含**入库后编辑与重归档。远期想法「记忆浏览器 / 可视化」在 HUMAN 待办，**未进 TODO**。

### 2.4 记忆保鲜与代谢 📐

**计划**（`TODO.md` 记忆系统调优段，长期、按使用体感调）
- 检索参数调优（`score_threshold` / `top_k` / 分级阈值）
- 相邻 chunk 合并（避免 LLM 看到被截断片段）
- **保鲜机制**：`last_hit_at` / `priority`，被频繁检索的记忆提升保留权重
- 向量同步补偿（ChromaDB 恢复后校验 SQLite ↔ Chroma 一致性并补写）
- Embedding 模型迁移脚本（重建 collection + 全量重索引，待触发）

### 2.5 用户画像 📐

**计划**（`TODO.md` 多场景 AI 架构段，标「长期」）：从记忆中提炼结构化用户画像，注入 system prompt；落地手段含长期记忆按 `source_role` 加权（软隔离）。

> 层级调整：原为顶层条目，四份意见一致认为它是**记忆的衍生物**（TODO 原文即从记忆提炼），降为记忆域子项。

### 2.6 知识库 🌱

**零承接**：TODO 无条目、`exchange/` 无讨论稿、无设计。与 2.2 的区别（四份意见一致强调）：长期记忆 = 对话自动沉淀（已实现）；知识库 = 用户主动导入的外部资料（RAG 同源、产品语义不同）。

> Trae 建议：要么标注「愿景，未排期」，要么给最小排期——防中途介入的读者误判为在建能力。

---

## 三、能力扩展

### 3.1 多模型按需切换 ✅

`config.yaml` 的 `models.<name>` 任意新增，`config.py` 自动解析，**加模型零代码**。当前为手动指定，无自动路由。

### 3.2 工具挂载 ✅

**已做**
- 框架：`tools/base.py`（BaseTool 护栏基类：超时强制 / 错误归一 ToolError / 执行日志）+ `registry.py` 注册 + `config.yaml` `enabled_tools` 开关
- 已挂：`shell` / `file_ops` / `web_search`
- `web_search`：Brave provider（可插拔），2026-09-04 真机验收通过（成功路径 + 关梯子单边挂降级路径双实测）

**计划（未定案）**：`web-search-design.md` 有多 provider 按角色路由方案（Tavily / Brave / Wolfram）；当前共识是**先用 Brave 攒真实体感**，换 provider 因封装可插拔成本已封死。

### 3.3 扩展性与零代码接入 ✅

四份意见一致列为「最值钱却无处安放的家底」之一。事实：
- 加模型 → config 加段（3.1）
- 加工具 → 写类 + 登记（3.2）；第三方挂载契约已留接口
- 加角色 → config `roles` 段 + 重启，`GET /api/roles` 下发，**前端零改动**（2026-09-03 改造验证）

**未做**：以上定制目前只能改 `config.yaml`，无 UI 设置页（见 1.1）。

---

## 四、专家与路由

### 4.1 角色（专家）切换 ✅

`config.yaml` `roles` 段 + `GET /api/roles` 动态下发 + 前端动态渲染（2026-09-03 改造，加角色零前端改动）。当前角色：default / code / health / finance / science。

**术语待定**：代码与 API 一律用 `role`（角色）；老大口述用「专家（或者叫角色）」。QoderWork 指出定位文档不宜出现「或者叫」。见 §待裁定。

**已知缺口**：会话级角色归属无数据模型（`TODO.md` 角色归属设计，老大暂缓）。

### 4.2 自动路由（意图识别） 📐

**计划**（`TODO.md` 多场景 AI 架构段，标「长期」）：对话开头快速分类意图、自动切换角色。同段另有「根据记忆向量空间分析自动建议新角色」。

---

## 五、边界与约束

> 老大原「约束」区只有成本 + 安全两条，四份意见一致认为偏薄、且混了不同性质。本域按**硬护栏 / 放权 / 凭据 / 成本 / 数据**五类拆开。

### 5.1 行为安全硬护栏 ✅

- `file_ops`：工作目录 `file_ops_workspace` 限制读写范围；文件不覆盖
- `shell`：命令黑名单 + IP 白名单 `shell_allowed_ips`
- `web_search`：SSRF 内网拦截 + 硬性超时
- 工具调用轮次上限（防失控循环）

**已知弱点**（`TODO.md` 部署段原文）：ShellTool 黑名单为**字符串包含匹配、可穿透**，加固随「本地能力下沉」一并做（正则/词法解析或换沙箱）。

### 5.2 边界透明与用户决策权 ✅

按 HUMAN 原则 4（明确安全边界，而非由系统替用户管控）：白名单 / 黑名单 / 工具开关**全部 config 可调**，边界交给用户决策；工具调用过程以 ToolCallCard 展示（见 6.2）。

### 5.3 凭据与密钥边界 ✅

`config.yaml` 含真实 key 且不入库（`.gitignore` 保护）、`config.example.yaml` 占位符；测试默认占位符、涉 key 路径需显式 `--real-api`；异常统一出口不泄漏内部信息；临时目录用后清理。

### 5.4 成本约束 🚧

**已做**：`token_counter` 统计 + `max_input_tokens` 截断 + 工具轮次上限。

**未做**：无预算 / 限额 / 告警。`TODO.md` 其他长期增强有「chat_service token 累计上限」，但老大标「优先级很低，不做主动处理」。

### 5.5 数据主权与出境边界 📐

**计划**（`exchange/deployment-architecture.md` 草案 + `TODO.md` 部署段）
- 数据落云厂商磁盘；Embedding 在自有服务器跑；**LLM API 是唯一出境通道**
- 上云硬前置（不满足别上）：强随机 `server.api_key`（P3.4 校验已就绪）/ HTTPS（Nginx TLS 终止）/ 上云初期显式禁用 `shell`+`file_ops`（`enabled_tools: ["web_search"]`），防云端误操作云机器

### 5.6 数据可恢复与可迁移 🚧

**已做（技术事实，未作产品承诺）**：SQLite 为真相源、ChromaDB 可重建 → 数据不绑架于向量库；会话与记忆均为软删除 + 回收站可恢复。

**未做**：整体备份 / 导出 / 迁移 — TODO 无条目。Marvis 与 Trae 均指出：上云后这半边会从「无所谓」变成核心承诺。

---

## 六、可见性与掌控

### 6.1 沉淀信息可见与可管理 🚧

同 2.3（本条为其产品表述）：已做 = 可看 / 可停用 / 可删 / 入库前可编辑；未做 = 入库后编辑与重归档。远期「记忆浏览器」在 `HUMAN.md` 待办，未进 TODO。

### 6.2 AI 行为可见 ✅

- **P4.4 ToolCallCard**：工具调用过程与结果在对话中展开（Marvis 定为「灵魂组件」）
- **降级提示**：搜索失败如实告知「未能联网核实」，不静默
- **来源标注**：`web_search` 回答标注来源 URL

### 6.3 资源消耗（成本）可见 🚧

**已做**：token 统计入库与用量日志（P3.3）。
**未做**：用户侧无展示。Marvis / QoderWork 均指出：原则 2「成本不能上天」若无可看见，管理是盲的。**当前无排期**。

---

## 七、质量与可靠性

### 7.1 时间感知 📐

专题讨论中：`exchange/discussion-time-context.md`（结论区 + 讨论区两段式，WB 维护）。当前第 2 轮——首轮意见已综合进结论区，待各方对结论区表态后定稿。**TODO 无条目**，落地方案以该稿结论区为准。

### 7.2 超长会话一致性 🚧

**现状**：`max_input_tokens` 截断旧消息 —— **机制方向是反的**（是丢弃，不是压缩）。这不是「做得不够好」，是方向做反。
**未做**：无 TODO 改造条目。改进方向目前仅在 `dsh-migration.md` 中作为 DSH compaction 借鉴点被提出，未立项。

### 7.3 降级与韧性 ✅

四份意见一致认为这是「做了大量工作却无处安放」的一块，本条为其归位：
- ChromaDB 不可用 → 静默降级，不阻塞
- `web_search` 失败 / 限流 → 指数退避 → 降级为正常回答 + 「未能联网核实」，不报错不中断（2026-09-04 关梯子单边挂真机实测通过）
- LLM 网络请求 tenacity 重试
- 异常统一出口：`LarryException` → 统一 JSON，不泄漏内部

---

## 八、形态与部署

### 8.1 云端部署、多端使用 📐

**计划**：`exchange/deployment-architecture.md`（草案，待老大确认后派发）；`TODO.md` 部署段：Nginx 脚本示例 / 部署文档 + 安全加固 / 租 VPS 跑 backend + SQLite + 小模型。移动端段：响应式 UI 或独立 `mobile/index.html`、PWA（可选）。

形态：PC 版 C/S（client 在本机不上云，server 上云）；移动版 B/S（浏览器直连云端）。

### 8.2 端侧能力保留（本地副作用工具在端上） 📐

**计划**（`TODO.md` 部署段，单独立项、不与云部署捆绑）：云端 backend 不注册 `shell` / `file_ops`，由本地执行器承接。下沉时一并完成 shell 鉴权重构（IP 白名单与公网不兼容，改 API Key 鉴权为主）+ 命令注入加固。

**产品理由**（Trae 主张提炼进形态层）：这是本项目选 C/S 而非纯 B/S 的存在理由——本地文件与命令能力只有端侧有。多端时该能力如何降级，是产品差异点。

### 8.3 单人单实例 ✅

形态事实（非能力）：不考虑多用户、并发、权限。QoderWork / Trae 指出这是决定无数设计的首要形态事实，此前只在设计原则里。

---

## 待裁定项（无落地计划，按规矩不写落地方式）

| 议题 | 各方意见 | 现状 |
|---|---|---|
| **主动触达**（定时 / 提醒 / AI 先开口） | QoderWork 存疑、Trae 建议无论做不做都先划界 | 全项目无任何规划痕迹 |
| **会话内多模态输入**（上传文件 / 图片） | Trae 建议定位层占位表态 | 无规划痕迹 |
| **术语统一**：专家 vs 角色 | QoderWork 指出「或者叫」不应出现在定位文档 | 代码与 API 一律 `role` |
| **是否单列「信任」域**（数据主权 + 可逆性 + 知情决策） | QoderWork 倾向立第六域；Trae 明确反对（分散归入约束 / 可见性即可）；Marvis 建议新增维度承载数据主权 + 可靠性 | 本稿采 Trae 方案：不立新域，拆入域五 / 域六 |
| **知识库去留** | Trae：标注愿景未排期，或给最小排期 | 保留为 🌱 并明示零承接 |

---

## 四份意见的采纳处理

| 提议 | 出处 | 处理 | 理由 |
|---|---|---|---|
| 记忆升格为带子结构的域 | Claude ①、QoderWork、Marvis | **采纳**（域二，6 子项） | 主线能力，一行装不下 |
| 加「降级与韧性」 | Claude ②、QoderWork 3 | **采纳**（7.3） | 已做工作量最大却无归属 |
| 加数据主权 / 出境边界 | 四份一致 | **采纳**（5.5） | 最大结构缺口 |
| 加数据生命周期（备份/导出/迁移） | Marvis 1、Trae 二.1 | **采纳**（5.6），但标未做 | 上云后转核心承诺 |
| 加「开放性 / 扩展成本」 | Claude ④、Marvis 3、QoderWork 4 | **采纳**（3.3） | 配置驱动是架构级事实 |
| 加「成本 / 用量可见」 | Marvis 5、QoderWork 7 | **采纳**（6.3），标未做无排期 | 与 5.4 成本管理配套 |
| 形态加「端侧能力保留」 | Trae 二.3、Marvis 6、QoderWork | **采纳**（8.2） | C/S 形态的产品级理由 |
| 形态加「单人单实例」 | QoderWork、Trae | **采纳**（8.3） | 首要形态事实 |
| 逐条标状态 | 四份一致 | **采纳**（状态图例 + 落地方式写实） | 防中途介入者误读现状 |
| 用户画像降为记忆子项 | QoderWork 5、Marvis 4、Trae | **采纳**（2.5） | TODO 原文即从记忆提炼 |
| 专家切换 / 自动路由拆开 | QoderWork 3、Claude ②⑥ | **采纳**（4.1 / 4.2） | 成熟度悬殊 |
| 安全约束内部拆层 | Claude ④、QoderWork 2 | **采纳**（5.1–5.3 三层） | 硬护栏 vs 透明放权性质不同 |
| 加「质量可评估性」（评估类代码） | Trae 一.3、QoderWork 5、Claude | **不采纳** | 老大 2026-09-06 裁定属**项目实施层** |
| 单列「信任」第六域 | QoderWork 倾向 | **不采纳**（采 Trae 方案） | 保持顶层为能力树，不混入抽象价值域 |
| 加可逆性 / 知情决策为一等维度 | QoderWork 2 | **折中**：可逆性落在 5.6 + 1.1 回收站；知情决策落在 6.2 | 同上 |

---

## 与旧版 14 条的编号映射

> 供引用旧编号的文档（`exchange/dsh-migration.md` §3 对照表）对照，避免两份真相。

| 旧 | 旧条目 | 新 |
|---|---|---|
| #1 | 多模型按需切换 | 3.1 |
| #2 | 工具挂载 | 3.2 |
| #3 | 专家切换和自动路由 | 4.1 + 4.2（拆） |
| #4 | 长期记忆和短期记忆 | 2.1 + 2.2 |
| #5 | 会话为单元管理 | 1.1 + 1.2 + 1.3（拆） |
| #6 | 用户画像 | 2.5（降为子项） |
| #7 | 知识库 | 2.6 |
| #8 | 使用成本管理 | 5.4（管理）+ 6.3（可见，拆） |
| #9 | 行为安全约束 | 5.1 + 5.2 + 5.3（拆三层） |
| #10 | 沉淀信息可见（可管理） | 6.1 |
| #11 | AI 行为可见 | 6.2 |
| #12 | 时间感知 | 7.1 |
| #13 | 良好的超长会话一致性 | 7.2 |
| #14 | 云端部署、多端使用 | 8.1 |
| — | **新增** | 1.3、2.4、3.3、4.2、5.3、5.5、5.6、6.3、7.3、8.2、8.3 |

---

## 变更记录

| 日期 | 变更 | 作者 |
|---|---|---|
| 2026-09-06 | 开稿：14 条升格为独立文件，重整为 8 域 / 29 子项树；综合 Claude / QoderWork / Marvis / Trae 四份意见，新增 11 项、拆分 4 项、降级 1 项；逐条写实落地方式（已做写做法、有计划写计划、**无承接不写**） | WB |
